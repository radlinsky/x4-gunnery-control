interface:
  display_name: "X4 Modding Research"
  short_description: "Evidence-backed X4 modding research"
  default_prompt: "Use $research-x4-modding to investigate and classify this X4 modding question; propose a KB update, and write it only when explicitly requested."
