<?xml version="1.0" encoding="UTF-8"?>
<!--
  Gunnery Control Test Lab - Live Scenario Spawner
  Developer-only.  Belongs in testlab, NOT in the shipped mod.

  Spawns the fixture described by testlab/x4_gunnery_control_testlab/ui/
  scenario_spec.lua. The directing agent edits and installs that spec; the
  owner presses Create test scenario after Lua preflights the exact ship and
  turret group. MD acknowledges the actual spawned count before Test Lab
  returns control.

  TRANSPORT
  Lua cannot hand MD a nested table safely: the only live-tested Lua->MD payload
  shape is a FLAT table of scalar keys, where the engine prepends '$' to every
  Lua string key (research KB, "Lua->MD AddUITriggeredEvent transport contract",
  live-tested 2026-08-04).  A multi-group spec is therefore streamed as a
  sequence of flat events rather than one nested payload:

    scenario_begin  {specId=, force=, requestId=, sectorMacro=,
                     anchorX=, anchorY=, anchorZ=}
    scenario_group  {macro=, faction=, count=, distance=, spread=,
                     behaviour="wait"|"attack"|"none", hostile=true|false,
                     label=, holdFire=, stripDefenceUnits=,
                     repairGuard=, yaw=, role=, loadout=,
                     geometryRole=, expectedWeapons=, expectedTurrets=,
                     expectedBeam=, expectedPlasma=,
                     expectedMissileTurrets=, expectedGuided=,
                     expectedDumbfire=, expectedAmmo=}        (repeated)
    scenario_station {recipe="xen_defence", faction=, distance=, spread=,
                      x=, y=, hostile=, label=, expectedModules=,
                      minSurfaces=, holdFire=, geometryRole=,
                      searchAttempts=, searchStepY=}          (repeated)
    scenario_commit {}

  Begin decides whether this spec should spawn at all (see IDEMPOTENCY), group
  events queue definitions into ScenarioRoot.$Pending, and commit despawns the
  previous fixture and creates the new one.  If Begin declines, the group and
  commit events are ignored because ScenarioRoot.$Accepting is false.

  IDEMPOTENCY
  Every Reload UI re-runs testlab.lua, so the spec is re-sent on every reload.
  ScenarioRoot.$SpawnedSpecId records the id of the fixture currently on the
  field.  scenario_begin refuses when the incoming specId equals it, so a
  Reload UI during an unrelated test cannot stack a second copy of the same
  fleet.  Changing `id` in the spec (or pressing the on-demand button, which
  sends force=true) is what makes it spawn again.  MD cue variables survive a
  UI reload - that is the property the whole gunnery persistence layer is built
  on (docs/RELOADING.md, "Two consequences worth knowing") - so this comparison
  still holds after the reload that triggers it.

  Despawn always precedes spawn on an accepted commit, so even a forced re-run
  replaces rather than piles up.

  Evidence citations
  create_ship / owner exact / loadout level:
    .x4-research-cache/extracted/scripts-9.00/md/rml_deliver_fleet.xml:251-259
    .x4-research-cache/extracted/scripts-9.00/md/tutorial_captureship.xml:180-183
    .x4-research-cache/extracted/scripts-9.00/md/lib_generic.xml:741-742

  static named loadout supplied while creating the shipped Colossus E:
    .x4-research-cache/extracted/issue67-vanilla-md-9.00/md/scenario_combat.xml:738-748
    .x4-research-cache/extracted/issue67-vanilla-md-9.00/libraries/loadouts.xml:405-451

  get_loadout: lookup a named loadout by ID string (loadout=) and optional
  defining macro (macro=) into result=; the shipped call site consumes the
  result via apply_loadout. Shipped source does NOT state what a missing ID
  leaves in result, or that execution even continues after it, so the
  pre-spawn probe logs a stage/result marker around each lookup instead of
  assuming anything about a failed lookup:
    .x4-research-cache/extracted/schemas-9.00/libraries/common.xsd:24489-24519
    .x4-research-cache/extracted/issue67-vanilla-md-9.00/md/showcases.xml:23

  safepos with a z distance plus a max scatter:
    .x4-research-cache/extracted/scripts-9.00/md/tutorial_map_missions.xml:523

  macro.{<string>} - resolving a UI-supplied macro NAME to a macro:
    .x4-research-cache/extracted/scripts-9.00/md/x4ep1_mentor_subscription.xml:9235-9236,9564

  faction.{<string>} - INFERRED, not directly attested.  `faction` is a lookup
  keyword exactly like `macro`
    (.x4-research-cache/extracted/props-9.00/libraries/scriptproperties.xml:2862-2864
     for macro, :2903-2907 for faction), and `lookup.faction.{$i}` indexing is
  attested at
    .x4-research-cache/extracted/scripts-9.00/md/gm_find_object.xml:1106-1108.
  The `?` guard below falls back to faction.xenon and logs, so a wrong faction
  string degrades to a visible log line rather than a broken cue.

  do_for_each over a list of tables, with optional-key access ($Def.$key?):
    .x4-research-cache/extracted/scripts-9.00/md/rml_deliver_fleet.xml:461-483

  append_to_list of a table literal:
    .x4-research-cache/extracted/scripts-9.00/md/factiongoal_plunder.xml:19

  add_relation_boost (hostile):
    .x4-research-cache/extracted/scripts-9.00/md/encounters.xml:300

  Wait order (stationary):
    .x4-research-cache/extracted/scripts-9.00/md/rml_repairobject.xml:290
    .x4-research-cache/extracted/scripts-9.00/md/lib_generic.xml:762

  Attack order:
    .x4-research-cache/extracted/scripts-9.00/md/encounters.xml:301-306

  do_for_each / destroy_object cleanup:
    .x4-research-cache/extracted/scripts-9.00/md/cpu_ship_manager.xml:879-894

  RELOAD NOTE: any change to THIS file requires a full game restart, because
  testlab/ md is only reloadable together with the rest of the extension.
  Editing scenario_spec.lua alone needs only install + Reload UI.
  See docs/RELOADING.md.
-->
<mdscript name="X4GunneryTestLabScenario" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../../../#modding/xsd/md.xsd">
  <cues>
    <cue name="ScenarioRoot">
      <cues>

        <!-- ScenarioBegin
             AddUITriggeredEvent("X4GunneryTestLabScenario", "scenario_begin",
               {specId = "...", force = true|false, requestId = "..."})
             Opens an accepting window and clears any half-built queue. -->
        <cue name="ScenarioBegin" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'scenario_begin'"/>
          </conditions>
          <actions>
            <set_value name="$SpecId" exact="if @event.param3.$specId then event.param3.$specId else 'unnamed'"/>
            <set_value name="$Force" exact="@event.param3.$force == true"/>
            <set_value name="$RequestId" exact="if @event.param3.$requestId then event.param3.$requestId else ''"/>

            <do_if value="(not $Force) and ScenarioRoot.$SpawnedSpecId? and ScenarioRoot.$SpawnedSpecId == $SpecId">
              <set_value name="ScenarioRoot.$Accepting" exact="false"/>
              <debug_text text="'[X4GC TEST SCENARIO] skipped; spec already spawned id=' + $SpecId"/>
            </do_if>
            <do_else>
              <set_value name="ScenarioRoot.$Accepting" exact="true"/>
              <set_value name="ScenarioRoot.$PendingSpecId" exact="$SpecId"/>
              <set_value name="ScenarioRoot.$PendingRequestId" exact="$RequestId"/>
              <set_value name="ScenarioRoot.$PendingSectorMacro" exact="if @event.param3.$sectorMacro then event.param3.$sectorMacro else ''"/>
              <set_value name="ScenarioRoot.$PendingAnchorX" exact="if @event.param3.$anchorX then event.param3.$anchorX else 0"/>
              <set_value name="ScenarioRoot.$PendingAnchorY" exact="if @event.param3.$anchorY then event.param3.$anchorY else 0"/>
              <set_value name="ScenarioRoot.$PendingAnchorZ" exact="if @event.param3.$anchorZ then event.param3.$anchorZ else 0"/>
              <set_value name="ScenarioRoot.$Pending" exact="[]"/>
              <set_value name="ScenarioRoot.$PendingStations" exact="[]"/>
              <debug_text text="'[X4GC TEST SCENARIO] accepting spec id=' + $SpecId + ' force=' + $Force"/>
            </do_else>
          </actions>
        </cue>

        <!-- ScenarioStation
             Streams the one station recipe currently proven from X4's shipped
             constructionplans.xml. The recipe name is deliberately narrow:
             an unknown plan must fail visibly rather than create another
             plausible-looking but unequipped shell. -->
        <cue name="ScenarioStation" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'scenario_station'"/>
            <check_value value="ScenarioRoot.$Accepting? and ScenarioRoot.$Accepting"/>
          </conditions>
          <actions>
            <append_to_list name="ScenarioRoot.$PendingStations" exact="table[
              $label           = if @event.param3.$label           then event.param3.$label           else 'station',
              $recipe          = if @event.param3.$recipe          then event.param3.$recipe          else '',
              $faction         = if @event.param3.$faction         then event.param3.$faction         else 'xenon',
              $distance        = if @event.param3.$distance        then event.param3.$distance        else 30000,
              $spread          = if @event.param3.$spread          then event.param3.$spread          else 0,
              $x               = if @event.param3.$x               then event.param3.$x               else 0,
              $y               = if @event.param3.$y               then event.param3.$y               else 0,
              $hostile         = @event.param3.$hostile == true,
              $holdfire        = @event.param3.$holdFire == true,
              $geometryrole    = if @event.param3.$geometryRole then event.param3.$geometryRole else '',
              $searchattempts  = if @event.param3.$searchAttempts then event.param3.$searchAttempts else 1,
              $searchstepy     = if @event.param3.$searchStepY then event.param3.$searchStepY else 0,
              $expectedmodules = if @event.param3.$expectedModules then event.param3.$expectedModules else 0,
              $minsurfaces      = if @event.param3.$minSurfaces     then event.param3.$minSurfaces     else 0
            ]"/>
          </actions>
        </cue>

        <!-- ScenarioGroup
             AddUITriggeredEvent("X4GunneryTestLabScenario", "scenario_group",
               {macro=, faction=, count=, distance=, spread=, x=, y=,
                behaviour=, hostile=, label=})
             One event per group in the spec.  Queued, not spawned.
             x = metres right of player ship (default 0), positive = right.
             y = metres above player ship (default 0), positive = up.
             z (distance) = metres forward of player ship, positive = forward. -->
        <cue name="ScenarioGroup" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'scenario_group'"/>
            <check_value value="ScenarioRoot.$Accepting? and ScenarioRoot.$Accepting"/>
          </conditions>
          <actions>
            <append_to_list name="ScenarioRoot.$Pending" exact="table[
              $label     = if @event.param3.$label     then event.param3.$label     else 'group',
              $macro     = if @event.param3.$macro     then event.param3.$macro     else '',
              $faction   = if @event.param3.$faction   then event.param3.$faction   else 'xenon',
              $count     = if @event.param3.$count     then event.param3.$count     else 1,
              $distance  = if @event.param3.$distance  then event.param3.$distance  else 5000,
              $spread    = if @event.param3.$spread    then event.param3.$spread    else 0,
              $x         = if @event.param3.$x         then event.param3.$x         else 0,
              $y         = if @event.param3.$y         then event.param3.$y         else 0,
              $behaviour = if @event.param3.$behaviour then event.param3.$behaviour else 'wait',
              $hostile   = @event.param3.$hostile == true,
              $holdfire  = @event.param3.$holdFire == true,
              $stripdefenceunits = @event.param3.$stripDefenceUnits == true,
              $repairguard = @event.param3.$repairGuard == true,
              $yaw       = if @event.param3.$yaw       then event.param3.$yaw       else 0,
              $role      = if @event.param3.$role      then event.param3.$role      else '',
              $loadout   = if @event.param3.$loadout   then event.param3.$loadout   else '',
              $geometryrole = if @event.param3.$geometryRole then event.param3.$geometryRole else '',
              $expectedweapons = if @event.param3.$expectedWeapons then event.param3.$expectedWeapons else 0,
              $expectedturrets = if @event.param3.$expectedTurrets then event.param3.$expectedTurrets else 0,
              $expectedbeam = if @event.param3.$expectedBeam then event.param3.$expectedBeam else 0,
              $expectedplasma = if @event.param3.$expectedPlasma then event.param3.$expectedPlasma else 0,
              $expectedmissileturrets = if @event.param3.$expectedMissileTurrets then event.param3.$expectedMissileTurrets else 0,
              $expectedguided = if @event.param3.$expectedGuided then event.param3.$expectedGuided else 0,
              $expecteddumbfire = if @event.param3.$expectedDumbfire then event.param3.$expectedDumbfire else 0,
              $expectedammo = if @event.param3.$expectedAmmo then event.param3.$expectedAmmo else 0
            ]"/>
          </actions>
        </cue>

        <!-- ScenarioCommit
             AddUITriggeredEvent("X4GunneryTestLabScenario", "scenario_commit")
             Despawns the previous fixture, then creates every queued group. -->
        <cue name="ScenarioCommit" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'scenario_commit'"/>
            <check_value value="ScenarioRoot.$Accepting? and ScenarioRoot.$Accepting"/>
            <check_value value="not (ScenarioRoot.$Spawned? and ScenarioRoot.$Spawned.indexof.{player.ship})"/>
          </conditions>
          <actions>
            <set_value name="ScenarioRoot.$Accepting" exact="false"/>

            <!-- Replace, never stack. -->
            <signal_cue_instantly cue="DespawnScenarioImpl"/>

            <create_group groupname="ScenarioRoot.$Spawned"/>
            <create_group groupname="ScenarioRoot.$SpawnedStations"/>
            <create_group groupname="ScenarioRoot.$SafeObjects"/>
            <create_group groupname="ScenarioRoot.$RepairObjects"/>
            <create_group groupname="ScenarioRoot.$PlayerShooters"/>
            <set_value name="$StationModules" exact="0"/>
            <set_value name="$StationTurrets" exact="0"/>
            <set_value name="$StationMissileTurrets" exact="0"/>
            <set_value name="$StationShields" exact="0"/>
            <set_value name="$StationEngines" exact="0"/>
            <set_value name="$StationLoadoutFailures" exact="0"/>
            <set_value name="$SafeObjectCount" exact="0"/>
            <set_value name="$SafeWeapons" exact="0"/>
            <set_value name="$UnsafeWeapons" exact="0"/>
            <set_value name="$DefenceUnits" exact="0"/>
            <set_value name="$Hostiles" exact="0"/>
            <set_value name="$RepairObjectCount" exact="0"/>
            <set_value name="$ShooterCount" exact="0"/>
            <set_value name="$ShooterWeapons" exact="0"/>
            <set_value name="$ShooterTurrets" exact="0"/>
            <set_value name="$ShooterBeam" exact="0"/>
            <set_value name="$ShooterPlasma" exact="0"/>
            <set_value name="$ShooterMissileTurrets" exact="0"/>
            <set_value name="$ShooterGuided" exact="0"/>
            <set_value name="$ShooterDumbfire" exact="0"/>
            <set_value name="$ShooterAmmo" exact="0"/>
            <set_value name="$LoadoutFailures" exact="0"/>
            <set_value name="$LocationFailures" exact="0"/>
            <set_value name="$DormantShooterWeapons" exact="0"/>
            <set_value name="$UnsafeDormantShooterWeapons" exact="0"/>
            <set_value name="$PreflightFailures" exact="0"/>
            <set_value name="$GeometrySplits" exact="0"/>
            <set_value name="$GeometryPending" exact="0"/>
            <set_value name="$GeometryShooter" exact="null"/>
            <set_value name="ScenarioRoot.$GeometryShooter" exact="null"/>
            <set_value name="ScenarioRoot.$GeometryStation" exact="null"/>
            <set_value name="ScenarioRoot.$StationLoadoutModule" exact="null"/>
            <set_value name="ScenarioRoot.$StationLoadoutRequestId" exact="''"/>
            <set_value name="$ClearArcTarget" exact="null"/>
            <set_value name="$BelowArcTarget" exact="null"/>
            <set_value name="$ClearArcRequested" exact="false"/>
            <set_value name="$BelowArcRequested" exact="false"/>

            <!-- Argon Prime's shipped macro is Cluster_14_Sector001_macro.
                 Absolute sector positions keep this fixture independent of the
                 save's current sector and exactly 500 km from the origin. -->
            <set_value name="$Remote" exact="ScenarioRoot.$PendingSectorMacro != ''"/>
            <!-- A local fixture is fired on by the ship at whose console Test
                 Lab was opened.  Remote fixtures add their spawned role=shooter
                 ship below.  One group therefore drives repair attribution in
                 both setup modes. -->
            <do_if value="not $Remote">
              <add_to_group groupname="ScenarioRoot.$PlayerShooters" object="player.ship"/>
            </do_if>
            <do_if value="$Remote">
              <set_value name="$SectorMacroName" exact="ScenarioRoot.$PendingSectorMacro"/>
              <do_if value="not @macro.{$SectorMacroName}">
                <set_value name="$LocationFailures" operation="add"/>
                <debug_text text="'[X4GC TEST SCENARIO] unknown remote sector macro=' + $SectorMacroName"/>
              </do_if>
              <do_else>
                <find_sector name="$ScenarioSector" macro="macro.{$SectorMacroName}" checkoperational="false"/>
                <do_if value="not $ScenarioSector">
                  <set_value name="$LocationFailures" operation="add"/>
                  <debug_text text="'[X4GC TEST SCENARIO] remote sector not found macro=' + $SectorMacroName"/>
                </do_if>
              </do_else>
            </do_if>

            <!-- Exact issue #65 shooter loadout, derived from the shipped
                 story_paranid_stranded_odysseus_E loadout. Every one of the 16
                 turret slots is a missile turret: 9 guided and 7 dumbfire.
                 Four ammunition types split the ship's exact 160 capacity. -->
            <create_loadout result="$Issue65OdysseusLoadout">
              <macros>
                <engine macro="engine_par_l_allround_01_mk1_macro" path="../con_engine_03"/>
                <engine macro="engine_par_l_allround_01_mk1_macro" path="../con_engine_02"/>
                <engine macro="engine_par_l_allround_01_mk1_macro" path="../con_engine_01"/>
                <weapon macro="weapon_par_l_destroyer_01_mk1_macro" path="../con_weapon_01"/>
                <weapon macro="weapon_par_l_destroyer_01_mk1_macro" path="../con_weapon_02"/>
                <shield macro="shield_par_l_standard_01_mk2_macro" path="../con_shieldgen_large_01"/>
                <shield macro="shield_par_l_standard_01_mk2_macro" path="../con_shieldgen_large_02"/>
              </macros>
              <groups>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_front_up_mid"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_rear_up_mid"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_front_down_down" exact="4"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_front_up_mid2" exact="2"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_rear_up_left"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_rear_up_right"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_front_down_mid"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_rear_down_right"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_rear_down_left"/>
                <shields macro="shield_par_m_standard_02_mk2_macro" group="group_rear_down_mid" exact="2"/>
                <turrets macro="turret_par_m_guided_02_mk1_macro" group="group_front_up_mid" exact="2" ammunition="missile_gen_m_guided_01_mk1_macro"/>
                <turrets macro="turret_par_m_guided_02_mk1_macro" group="group_rear_up_left" exact="2" ammunition="missile_gen_m_guided_01_mk1_macro"/>
                <turrets macro="turret_par_m_guided_02_mk1_macro" group="group_rear_up_right" exact="2" ammunition="missile_gen_m_guided_01_mk1_macro"/>
                <turrets macro="turret_par_m_dumbfire_02_mk1_macro" group="group_front_down_mid" exact="2" ammunition="missile_gen_m_dumbfire_01_mk1_macro"/>
                <turrets macro="turret_par_m_dumbfire_02_mk1_macro" group="group_rear_down_right" exact="2" ammunition="missile_gen_m_dumbfire_01_mk1_macro"/>
                <turrets macro="turret_par_m_dumbfire_02_mk1_macro" group="group_rear_down_left" exact="2" ammunition="missile_gen_m_dumbfire_01_mk1_macro"/>
                <turrets macro="turret_par_l_guided_01_mk1_macro" group="group_front_down_down" exact="2" ammunition="missile_gen_l_guided_01_mk1_macro"/>
                <turrets macro="turret_par_l_guided_01_mk1_macro" group="group_front_up_mid2" ammunition="missile_gen_l_guided_01_mk1_macro"/>
                <turrets macro="turret_par_l_dumbfire_01_mk1_macro" group="group_rear_down_mid" ammunition="missile_gen_l_dumbfire_01_mk1_macro"/>
              </groups>
              <ammunition>
                <ammunition macro="missile_gen_m_guided_01_mk1_macro" exact="40"/>
                <ammunition macro="missile_gen_m_dumbfire_01_mk1_macro" exact="40"/>
                <ammunition macro="missile_gen_l_guided_01_mk1_macro" exact="40"/>
                <ammunition macro="missile_gen_l_dumbfire_01_mk1_macro" exact="40"/>
              </ammunition>
              <software>
                <software ware="software_dockmk2"/>
                <software ware="software_flightassistmk1"/>
                <software ware="software_scannerlongrangemk2"/>
                <software ware="software_scannerobjectmk2"/>
                <software ware="software_targetmk1"/>
                <software ware="software_trademk1"/>
              </software>
              <virtualmacros>
                <thruster macro="thruster_gen_l_allround_01_mk3_macro"/>
              </virtualmacros>
            </create_loadout>
            <do_for_each name="$Def" in="ScenarioRoot.$Pending">
              <do_if value="$Def.$geometryrole == 'clear_arc'">
                <set_value name="$ClearArcRequested" exact="true"/>
              </do_if>
              <do_elseif value="$Def.$geometryrole == 'below_arc'">
                <set_value name="$BelowArcRequested" exact="true"/>
              </do_elseif>
              <do_if value="$Def.$macro == '' or not @macro.{$Def.$macro}">
                <debug_text text="'[X4GC TEST SCENARIO] skipping group ' + $Def.$label + '; unknown macro=' + $Def.$macro"/>
                <continue/>
              </do_if>

              <set_value name="$Owner" exact="@faction.{$Def.$faction}"/>
              <do_if value="not $Owner">
                <debug_text text="'[X4GC TEST SCENARIO] unknown faction=' + $Def.$faction + ' for group ' + $Def.$label + '; using xenon'"/>
                <set_value name="$Owner" exact="faction.xenon"/>
              </do_if>

              <!-- Clamp to something a live test can actually manage. -->
              <set_value name="$Count" exact="$Def.$count"/>
              <do_if value="$Count lt 1"><set_value name="$Count" exact="1"/></do_if>
              <do_if value="$Count gt 12"><set_value name="$Count" exact="12"/></do_if>

              <set_value name="$Index" exact="0"/>
              <do_all exact="$Count">
                <set_value name="$Index" operation="add"/>

                <!-- Issue #67 pre-spawn diagnostic. The static-ref route was later
                     live-proven (SHA d7e3870) to apply its full per-group census
                     (weapons=4/turrets=4/beam=2/plasma=2); an earlier 2026-08-23
                     run had ended with 0 weapons/turrets, unexplained and
                     superseded. Shipped source proves the get_loadout
                     syntax and one successful use, but NOT what a missing ID
                     leaves in result - or that execution even continues after
                     it - so each lookup is bracketed by its own stage/result
                     log and the live run is read from the LAST reached marker:
                       no control result log            -> control lookup did not return; probe invalid
                       control ok, no custom result log -> failure is at the custom lookup stage
                       both results, custom not_resolved -> custom registration/lookup failure
                       both resolved                    -> lookup fine; failure is later create/apply
                     The shipped control is probed FIRST, so a custom-ID abort
                     can never hide its own result. Read-only: nothing here
                     branches on the results, so creation proceeds exactly as
                     before. -->
                <do_if value="$Def.$loadout == 'issue67_colossus_arc_barrel'">
                  <get_loadout result="$Issue67ProbeControl" loadout="'scenario_combat_arg_carrier_02'" macro="macro.{$Def.$macro}"/>
                  <set_value name="$Issue67ProbeControlOk" exact="$Issue67ProbeControl? and $Issue67ProbeControl != null"/>
                  <debug_text text="'[X4GC TEST SCENARIO] issue67 get_loadout stage=control_result control=' + (if $Issue67ProbeControlOk then 'resolved' else 'not_resolved') + ' macro=' + $Def.$macro"/>
                  <debug_text text="'[X4GC TEST SCENARIO] issue67 get_loadout stage=custom_start macro=' + $Def.$macro"/>
                  <get_loadout result="$Issue67ProbeCustom" loadout="'x4gc_testlab_issue67_colossus_arc_barrel'" macro="macro.{$Def.$macro}"/>
                  <set_value name="$Issue67ProbeCustomOk" exact="$Issue67ProbeCustom? and $Issue67ProbeCustom != null"/>
                  <debug_text text="'[X4GC TEST SCENARIO] issue67 get_loadout stage=custom_result custom=' + (if $Issue67ProbeCustomOk then 'resolved' else 'not_resolved') + ' macro=' + $Def.$macro"/>
                  <debug_text text="'[X4GC TEST SCENARIO] issue67 get_loadout stage=summary macro=' + $Def.$macro + ' custom=' + (if $Issue67ProbeCustomOk then 'resolved' else 'not_resolved') + ' control=' + (if $Issue67ProbeControlOk then 'resolved' else 'not_resolved')"/>
                </do_if>
                <do_if value="$Remote">
                  <do_if value="not $ScenarioSector">
                    <continue/>
                  </do_if>
                  <do_if value="$Def.$loadout == 'issue67_colossus_arc_barrel'">
                    <create_ship name="$Ship" groupname="ScenarioRoot.$Spawned"
                                 sector="$ScenarioSector" macro="macro.{$Def.$macro}">
                      <owner exact="$Owner"/>
                      <loadout ref="x4gc_testlab_issue67_colossus_arc_barrel"/>
                      <!-- A captain is required for the player to man a secondary
                           bridge post (the gunner console); without one the console
                           is inert. Mirrors vanilla scenario_combat's crewed Colossus. -->
                      <pilot><select faction="$Owner" tags="tag.pilot"/></pilot>
                      <position x="ScenarioRoot.$PendingAnchorX + $Def.$x"
                                y="ScenarioRoot.$PendingAnchorY + $Def.$y"
                                z="ScenarioRoot.$PendingAnchorZ + $Def.$distance"/>
                      <rotation yaw="($Def.$yaw)deg"/>
                    </create_ship>
                  </do_if>
                  <do_else>
                    <create_ship name="$Ship" groupname="ScenarioRoot.$Spawned"
                                 sector="$ScenarioSector" macro="macro.{$Def.$macro}">
                      <owner exact="$Owner"/>
                      <loadout><level exact="1.0"/></loadout>
                      <position x="ScenarioRoot.$PendingAnchorX + $Def.$x"
                                y="ScenarioRoot.$PendingAnchorY + $Def.$y"
                                z="ScenarioRoot.$PendingAnchorZ + $Def.$distance"/>
                      <rotation yaw="($Def.$yaw)deg"/>
                    </create_ship>
                  </do_else>
                </do_if>
                <do_else>
                  <do_if value="$Def.$loadout == 'issue67_colossus_arc_barrel'">
                    <create_ship name="$Ship" groupname="ScenarioRoot.$Spawned"
                                 zone="player.zone" macro="macro.{$Def.$macro}">
                      <owner exact="$Owner"/>
                      <loadout ref="x4gc_testlab_issue67_colossus_arc_barrel"/>
                      <safepos object="player.ship" x="$Def.$x" y="$Def.$y" z="$Def.$distance" max="$Def.$spread"/>
                      <rotation yaw="($Def.$yaw)deg"/>
                    </create_ship>
                  </do_if>
                  <do_else>
                    <create_ship name="$Ship" groupname="ScenarioRoot.$Spawned"
                                 zone="player.zone" macro="macro.{$Def.$macro}">
                      <owner exact="$Owner"/>
                      <loadout><level exact="1.0"/></loadout>
                      <safepos object="player.ship" x="$Def.$x" y="$Def.$y" z="$Def.$distance" max="$Def.$spread"/>
                      <rotation yaw="($Def.$yaw)deg"/>
                    </create_ship>
                  </do_else>
                </do_else>

                <do_if value="$Def.$loadout == 'issue65_odysseus_mixed_missiles'">
                  <apply_loadout object="$Ship" loadout="$Issue65OdysseusLoadout"/>
                </do_if>
                <do_elseif value="$Def.$loadout != '' and $Def.$loadout != 'issue67_colossus_arc_barrel'">
                  <set_value name="$LoadoutFailures" operation="add"/>
                  <debug_text text="'[X4GC TEST SCENARIO] unknown deterministic loadout=' + $Def.$loadout + ' ship=' + $Ship"/>
                </do_elseif>
                <do_if value="$Def.$loadout == 'issue67_colossus_arc_barrel'">
                  <debug_text text="'[X4GC TEST SCENARIO] issue67 loadout_transport=static_ref slot_paths=con_turret_m_06,con_turret_m_14,con_turret_m_08,con_turret_m_16 ship=' + $Ship"/>
                </do_if>

                <!-- Live tests kept failing on "which of these three identical Xenon fighters
                     am I supposed to designate" - an explicit name makes the instruction
                     unambiguous in the target list. -->
                <set_object_name object="$Ship" name="$Def.$label + ' ' + $Index"/>
                <do_if value="$Def.$geometryrole == 'clear_arc'">
                  <set_value name="$ClearArcTarget" exact="$Ship"/>
                </do_if>
                <do_elseif value="$Def.$geometryrole == 'below_arc'">
                  <set_value name="$BelowArcTarget" exact="$Ship"/>
                </do_elseif>

                <!-- A remotely created ship receives a dynamic zone at the
                     requested sector position.  component.position is local
                     to that zone, so convert it back to sector coordinates
                     before validating the absolute anchor. -->
                <do_if value="$Remote">
                  <create_position name="$ShipSectorPosition" object="$Ship" space="$ScenarioSector"/>
                </do_if>
                <do_if value="$Remote and ($Ship.sector != $ScenarioSector
                              or $ShipSectorPosition.x lt ScenarioRoot.$PendingAnchorX + $Def.$x - 10m
                              or $ShipSectorPosition.x gt ScenarioRoot.$PendingAnchorX + $Def.$x + 10m
                              or $ShipSectorPosition.y lt ScenarioRoot.$PendingAnchorY + $Def.$y - 10m
                              or $ShipSectorPosition.y gt ScenarioRoot.$PendingAnchorY + $Def.$y + 10m
                              or $ShipSectorPosition.z lt ScenarioRoot.$PendingAnchorZ + $Def.$distance - 10m
                              or $ShipSectorPosition.z gt ScenarioRoot.$PendingAnchorZ + $Def.$distance + 10m)">
                  <set_value name="$LocationFailures" operation="add"/>
                  <debug_text text="'[X4GC TEST SCENARIO] placement mismatch ship=' + $Ship + ' sector_x=' + $ShipSectorPosition.x + ' sector_y=' + $ShipSectorPosition.y + ' sector_z=' + $ShipSectorPosition.z"/>
                </do_if>

                <do_if value="$Def.$role == 'shooter'">
                  <set_value name="$ShooterCount" operation="add"/>
                  <add_to_group groupname="ScenarioRoot.$PlayerShooters" object="$Ship"/>
                  <set_value name="$GeometryShooter" exact="$Ship"/>
                  <!-- Persist the exact shooter so the in-system GeometryQualify
                       cue can re-measure station B against these same turrets
                       after the owner teleports aboard. -->
                  <set_value name="ScenarioRoot.$GeometryShooter" exact="$Ship"/>
                  <set_value name="$ShooterWeapons" operation="add" exact="$Ship.weapons.operational.count"/>
                  <set_value name="$ShooterTurrets" operation="add" exact="$Ship.turrets.operational.count"/>
                  <set_value name="$LocalBeam" exact="0"/>
                  <set_value name="$LocalPlasma" exact="0"/>
                  <do_for_each name="$Weapon" in="$Ship.turrets.operational.list">
                    <do_if value="$Weapon.macro == macro.turret_arg_m_beam_02_mk1_macro">
                      <set_value name="$LocalBeam" operation="add"/>
                    </do_if>
                    <do_elseif value="$Weapon.macro == macro.turret_arg_m_plasma_02_mk1_macro">
                      <set_value name="$LocalPlasma" operation="add"/>
                    </do_elseif>
                  </do_for_each>
                  <set_value name="$ShooterBeam" operation="add" exact="$LocalBeam"/>
                  <set_value name="$ShooterPlasma" operation="add" exact="$LocalPlasma"/>
                  <set_value name="$ShooterMissileTurrets" operation="add" exact="$Ship.missileturrets.operational.count"/>
                  <set_value name="$LocalGuided" exact="0"/>
                  <set_value name="$LocalDumbfire" exact="0"/>
                  <do_for_each name="$Weapon" in="$Ship.missileturrets.operational.list">
                    <do_if value="$Weapon.ammo.macro and $Weapon.ammo.macro.isguided">
                      <set_value name="$LocalGuided" operation="add"/>
                    </do_if>
                    <do_elseif value="$Weapon.ammo.macro">
                      <set_value name="$LocalDumbfire" operation="add"/>
                    </do_elseif>
                  </do_for_each>
                  <set_value name="$ShooterGuided" operation="add" exact="$LocalGuided"/>
                  <set_value name="$ShooterDumbfire" operation="add" exact="$LocalDumbfire"/>
                  <set_value name="$LocalAmmo" exact="$Ship.ammostorage.{macro.missile_gen_m_guided_01_mk1_macro}.count
                    + $Ship.ammostorage.{macro.missile_gen_m_dumbfire_01_mk1_macro}.count
                    + $Ship.ammostorage.{macro.missile_gen_l_guided_01_mk1_macro}.count
                    + $Ship.ammostorage.{macro.missile_gen_l_dumbfire_01_mk1_macro}.count"/>
                  <set_value name="$ShooterAmmo" operation="add" exact="$LocalAmmo"/>
                  <do_if value="$Ship.weapons.operational.count != $Def.$expectedweapons
                                or $Ship.turrets.operational.count != $Def.$expectedturrets
                                or $LocalBeam != $Def.$expectedbeam
                                or $LocalPlasma != $Def.$expectedplasma
                                or $Ship.missileturrets.operational.count != $Def.$expectedmissileturrets
                                or $LocalGuided != $Def.$expectedguided
                                or $LocalDumbfire != $Def.$expecteddumbfire
                                or $LocalAmmo != $Def.$expectedammo">
                    <set_value name="$LoadoutFailures" operation="add"/>
                    <debug_text text="'[X4GC TEST SCENARIO] shooter loadout mismatch ship=' + $Ship + ' weapons=' + $Ship.weapons.operational.count + ' turrets=' + $Ship.turrets.operational.count + ' beam=' + $LocalBeam + ' plasma=' + $LocalPlasma + ' missile_turrets=' + $Ship.missileturrets.operational.count + ' guided=' + $LocalGuided + ' dumbfire=' + $LocalDumbfire + ' ammo=' + $LocalAmmo"/>
                  </do_if>
                  <!-- Keep the remote shooter dormant while the owner
                       teleports. Direct-control engagement owns the selected
                       groups and temporarily overrides this HOLD FIRE baseline;
                       no safety guard reasserts it during the actual trial. -->
                  <cease_fire object="$Ship"/>
                  <do_for_each name="$Weapon" in="$Ship.turrets.operational.list">
                    <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                    <do_if value="$Weapon.mode == weaponmode.holdfire">
                      <set_value name="$DormantShooterWeapons" operation="add"/>
                    </do_if>
                    <do_else>
                      <set_value name="$UnsafeDormantShooterWeapons" operation="add"/>
                    </do_else>
                  </do_for_each>
                  <do_for_each name="$Weapon" in="$Ship.missileturrets.operational.list">
                    <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                    <do_if value="$Weapon.mode == weaponmode.holdfire">
                      <set_value name="$DormantShooterWeapons" operation="add"/>
                    </do_if>
                    <do_else>
                      <set_value name="$UnsafeDormantShooterWeapons" operation="add"/>
                    </do_else>
                  </do_for_each>
                </do_if>

                <!-- Remove combat drones before hostility is applied. A
                     launched drone is another hostile target and invalidates
                     per-surface projectile attribution even if it crosses the
                     selected component's bearing only briefly. -->
                <do_if value="$Def.$stripdefenceunits and $Ship.units.{unitcategory.defence}.count">
                  <remove_units object="$Ship" category="unitcategory.defence" mk="1"
                                exact="$Ship.units.{unitcategory.defence}.count"/>
                </do_if>
                <set_value name="$DefenceUnits" operation="add"
                           exact="$Ship.units.{unitcategory.defence}.count"/>

                <do_if value="$Def.$repairguard">
                  <set_value name="$RepairObjectCount" operation="add"/>
                  <add_to_group groupname="ScenarioRoot.$RepairObjects" object="$Ship"/>
                </do_if>

                <do_if value="$Def.$holdfire">
                  <set_value name="$SafeObjectCount" operation="add"/>
                  <add_to_group groupname="ScenarioRoot.$SafeObjects" object="$Ship"/>
                  <cease_fire object="$Ship"/>
                  <do_for_each name="$Weapon" in="$Ship.turrets.operational.list">
                    <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                    <do_if value="$Weapon.mode == weaponmode.holdfire">
                      <set_value name="$SafeWeapons" operation="add"/>
                    </do_if>
                    <do_else><set_value name="$UnsafeWeapons" operation="add"/></do_else>
                  </do_for_each>
                  <do_for_each name="$Weapon" in="$Ship.missileturrets.operational.list">
                    <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                    <do_if value="$Weapon.mode == weaponmode.holdfire">
                      <set_value name="$SafeWeapons" operation="add"/>
                    </do_if>
                    <do_else><set_value name="$UnsafeWeapons" operation="add"/></do_else>
                  </do_for_each>
                </do_if>

                <!-- Where a ship actually ended up is the first thing anyone
                     asks when it cannot be found: safepos may move it, and a
                     null player.zone would place it somewhere unexpected. -->
                <debug_text text="'[X4GC TEST SCENARIO] created ' + $Ship + ' exists=' + $Ship.exists + ' sector=' + $Ship.sector.knownname + ' dist=' + $Ship.distanceto.{player.ship} + ' playersector=' + player.sector.knownname + ' name=' + $Ship.knownname"/>

                <do_if value="$Def.$hostile">
                  <set_value name="$HostileReference" exact="if ScenarioRoot.$PlayerShooters.count then ScenarioRoot.$PlayerShooters.{1} else player.ship"/>
                  <add_relation_boost object="$Ship" otherobject="$HostileReference"
                                      value="$Ship.owner.relation.kill.max"
                                      decay="1" delay="60min" silent="true"/>
                  <do_if value="$HostileReference.mayattack.{$Ship}">
                    <set_value name="$Hostiles" operation="add"/>
                  </do_if>
                </do_if>

                <do_if value="$Def.$behaviour == 'wait'">
                  <create_order id="'Wait'" object="$Ship" default="true"/>
                </do_if>
                <do_elseif value="$Def.$behaviour == 'attack'">
                  <create_order id="'Attack'" object="$Ship" default="true">
                    <param name="primarytarget" value="player.ship"/>
                    <param name="pursuetargets" value="true"/>
                  </create_order>
                </do_elseif>
              </do_all>

              <debug_text text="'[X4GC TEST SCENARIO] group ' + $Def.$label + ' macro=' + $Def.$macro + ' faction=' + $Def.$faction + ' count=' + $Count + ' distance=' + $Def.$distance + ' behaviour=' + $Def.$behaviour"/>
            </do_for_each>

            <!-- ScenarioStation consumer (#67 station B).  Create one preserved
                 station remotely, then run a deliberately narrow station-module
                 loadout discriminator.  common.xsd documents apply_loadout
                 object= for a ship OR a station MODULE, not a station root.  Apply
                 the deterministic standard loadout to one exact operational defence
                 module and observe the station census immediately, after a
                 delayed MD cue, and again in system.  Those observations are the
                 experiment; no timing or remote-equipment conclusion is assumed
                 here. -->
            <do_for_each name="$Def" in="ScenarioRoot.$PendingStations">
              <do_if value="$Def.$recipe != 'xen_defence'">
                <debug_text text="'[X4GC TEST SCENARIO] skipping station ' + $Def.$label + '; unknown recipe=' + $Def.$recipe"/>
                <continue/>
              </do_if>
              <!-- ponytail: #67 station B is remote-only; no local zone fallback. -->
              <do_if value="not ($Remote and $ScenarioSector)">
                <set_value name="$LocationFailures" operation="add"/>
                <debug_text text="'[X4GC TEST SCENARIO] station ' + $Def.$label + ' needs a resolved remote sector; skipping'"/>
                <continue/>
              </do_if>

              <set_value name="$Owner" exact="@faction.{$Def.$faction}"/>
              <do_if value="not $Owner">
                <set_value name="$Owner" exact="faction.xenon"/>
              </do_if>
              <!-- Two-phase #67 station B.  Out of system at ScenarioCommit time
                   the turret aim target does not resolve to a real hull surface,
                   so root and hittable-aim pitch are near-identical (~1e-6 rad
                   apart in the live run) and no root-outside/aim-inside split can
                   be measured here; the old bounded OOS search only ever destroyed
                   equivalent shells.  Preserve exactly ONE station at the attempt-0
                   position, verify its census, and mark geometry PENDING.  The
                   discriminating split is re-measured IN SYSTEM by GeometryQualify
                   after the owner teleports to the Colossus; nothing here reports
                   the fixture as geometrically ready. -->
              <set_value name="$AimSplit" exact="$Def.$geometryrole == 'aim_split'"/>
              <create_station name="$Station"
                              sector="$ScenarioSector" macro="macro.station_gen_factory_base_01_macro"
                              constructionplan="'xen_defence'" owner="$Owner"
                              state="componentstate.operational">
                <position x="ScenarioRoot.$PendingAnchorX + $Def.$x"
                          y="ScenarioRoot.$PendingAnchorY + $Def.$y"
                          z="ScenarioRoot.$PendingAnchorZ + $Def.$distance"/>
              </create_station>
              <!-- Safe deterministic discriminator loadout: one defence module,
                   two non-integrated standard medium lasers in group01, one
                   medium shield in each of groups01-04, and no large group05
                   equipment. -->
              <create_loadout result="$StationLoadout">
                <groups>
                  <turrets macro="turret_xen_m_laser_02_mk1_macro" group="group01" exact="2"/>
                  <shields macro="shield_xen_m_standard_02_mk1_macro" group="group01"/>
                  <shields macro="shield_xen_m_standard_02_mk1_macro" group="group02"/>
                  <shields macro="shield_xen_m_standard_02_mk1_macro" group="group03"/>
                  <shields macro="shield_xen_m_standard_02_mk1_macro" group="group04"/>
                </groups>
              </create_loadout>
              <set_value name="$StationLoadoutModule" exact="null"/>
              <do_for_each name="$Module" in="$Station.modules.operational.list">
                <do_if value="not $StationLoadoutModule and $Module.macro == macro.xenon_small_station_01_base_macro">
                  <set_value name="$StationLoadoutModule" exact="$Module"/>
                </do_if>
              </do_for_each>
              <do_if value="$StationLoadoutModule">
                <apply_loadout object="$StationLoadoutModule" loadout="$StationLoadout"/>
                <set_value name="ScenarioRoot.$StationLoadoutModule" exact="$StationLoadoutModule"/>
                <set_value name="ScenarioRoot.$StationLoadoutRequestId" exact="ScenarioRoot.$PendingRequestId"/>
              </do_if>
              <do_else>
                <set_value name="$StationLoadoutFailures" operation="add"/>
                <debug_text text="'[X4GC TEST STATION LOADOUT] phase=apply FAILED reason=no_matching_module station=' + $Station + ' modules=' + $Station.modules.operational.count"/>
              </do_else>
              <add_to_group groupname="ScenarioRoot.$SpawnedStations" object="$Station"/>
              <set_object_name object="$Station" name="$Def.$label"/>
              <do_if value="$AimSplit">
                <set_value name="ScenarioRoot.$GeometryStation" exact="$Station"/>
                <set_value name="$GeometryPending" operation="add"/>
                <debug_text text="'[X4GC TEST SCENARIO] station ' + $Def.$label + ' preserved at attempt-0 for in-system qualification; geometry pending station=' + $Station + '; teleport to the Colossus and open Test Lab once'"/>
              </do_if>

              <set_value name="$ImmediateLasers" exact="0"/>
              <do_for_each name="$Weapon" in="$Station.turrets.operational.list">
                <do_if value="$Weapon.macro == macro.turret_xen_m_laser_02_mk1_macro">
                  <set_value name="$ImmediateLasers" operation="add"/>
                </do_if>
              </do_for_each>
              <debug_text text="'[X4GC TEST STATION LOADOUT] phase=immediate request_id=' + ScenarioRoot.$PendingRequestId + ' station=' + $Station + ' module=' + $StationLoadoutModule + ' module_macro=' + (if $StationLoadoutModule then $StationLoadoutModule.macro else null) + ' modules=' + $Station.modules.operational.count + ' turrets=' + $Station.turrets.operational.count + ' standard_lasers=' + $ImmediateLasers + ' missile_turrets=' + $Station.missileturrets.operational.count + ' shields=' + $Station.shields.operational.count + ' engines=' + $Station.engines.operational.count"/>
              <signal_cue cue="StationLoadoutDelayedCensus"/>

              <!-- Strip carried defence drones before hostility. -->
              <do_if value="$Station.units.{unitcategory.defence}.count">
                <remove_units object="$Station" category="unitcategory.defence" mk="1"
                              exact="$Station.units.{unitcategory.defence}.count"/>
              </do_if>
              <set_value name="$DefenceUnits" operation="add"
                         exact="$Station.units.{unitcategory.defence}.count"/>

              <do_if value="$Def.$hostile">
                <add_relation_boost object="$Station" otherobject="player.ship"
                                    value="$Station.owner.relation.kill.max"
                                    decay="1" delay="60min" silent="true"/>
                <do_if value="player.ship.mayattack.{$Station}">
                  <set_value name="$Hostiles" operation="add"/>
                </do_if>
              </do_if>
              <signal_objects object="player.galaxy" param="'init station'" param2="$Station" param3="true"/>

              <!-- A hostile surface fixture must never return fire: keep the
                   weapon surfaces installed for the arc test but hold every
                   emitter and stop any queued shot.  ScenarioSafetyGuard
                   reasserts this for as long as the station exists. -->
              <do_if value="$Def.$holdfire">
                <set_value name="$SafeObjectCount" operation="add"/>
                <add_to_group groupname="ScenarioRoot.$SafeObjects" object="$Station"/>
                <cease_fire object="$Station"/>
                <do_for_each name="$Weapon" in="$Station.turrets.operational.list">
                  <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                  <do_if value="$Weapon.mode == weaponmode.holdfire">
                    <set_value name="$SafeWeapons" operation="add"/>
                  </do_if>
                  <do_else><set_value name="$UnsafeWeapons" operation="add"/></do_else>
                </do_for_each>
                <do_for_each name="$Weapon" in="$Station.missileturrets.operational.list">
                  <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                  <do_if value="$Weapon.mode == weaponmode.holdfire">
                    <set_value name="$SafeWeapons" operation="add"/>
                  </do_if>
                  <do_else><set_value name="$UnsafeWeapons" operation="add"/></do_else>
                </do_for_each>
              </do_if>

              <set_value name="$StationModules" operation="add" exact="$Station.modules.operational.count"/>
              <set_value name="$StationTurrets" operation="add" exact="$Station.turrets.operational.count"/>
              <set_value name="$StationMissileTurrets" operation="add" exact="$Station.missileturrets.operational.count"/>
              <set_value name="$StationShields" operation="add" exact="$Station.shields.operational.count"/>
              <set_value name="$StationEngines" operation="add" exact="$Station.engines.operational.count"/>
              <debug_text text="'[X4GC TEST SCENARIO] created station=' + $Station + ' name=' + $Station.knownname + ' recipe=' + $Def.$recipe + ' geometry_pending=' + $AimSplit + ' modules=' + $Station.modules.operational.count + ' turrets=' + $Station.turrets.operational.count + ' shields=' + $Station.shields.operational.count + ' engines=' + $Station.engines.operational.count"/>
            </do_for_each>

            <!-- Fail-closed controls for #67.  READY requires both exact
                 conventional turrets to see the clear P as in-range, inside
                 both origin and hittable-aim arcs, and externally unmasked.
                 The below-arc P must remain in range while both bearings are
                 below the shared -10 degree lower elevation stop. -->
            <create_list name="$GeometryControls"/>
            <do_if value="$ClearArcTarget">
              <append_to_list name="$GeometryControls" exact="table[$role = 'clear_arc', $target = $ClearArcTarget]"/>
            </do_if>
            <do_elseif value="$ClearArcRequested">
              <set_value name="$PreflightFailures" operation="add"/>
            </do_elseif>
            <do_if value="$BelowArcTarget">
              <append_to_list name="$GeometryControls" exact="table[$role = 'below_arc', $target = $BelowArcTarget]"/>
            </do_if>
            <do_elseif value="$BelowArcRequested">
              <set_value name="$PreflightFailures" operation="add"/>
            </do_elseif>
            <do_for_each name="$Control" in="$GeometryControls">
              <set_value name="$PassingWeapons" exact="0"/>
              <do_if value="$GeometryShooter">
                <do_for_each name="$GeometryWeapon" in="$GeometryShooter.turrets.operational.list">
                  <do_if value="$GeometryWeapon.macro == macro.turret_arg_m_beam_02_mk1_macro
                                or $GeometryWeapon.macro == macro.turret_arg_m_plasma_02_mk1_macro">
                    <set_value name="$RootRelative" exact="$Control.$target.relativeposition.{$GeometryWeapon}"/>
                    <create_orientation name="$AimLocal" orientation="look_at"
                                        refobject="$Control.$target" useaimtarget="true">
                      <position object="$GeometryWeapon" space="$GeometryWeapon"/>
                    </create_orientation>
                    <set_value name="$RootArcPass" exact="$RootRelative.rotation.pitch ge -10deg and $RootRelative.rotation.pitch le 90deg"/>
                    <set_value name="$AimArcPass" exact="$AimLocal.pitch ge -10deg and $AimLocal.pitch le 90deg"/>
                    <set_value name="$InRange" exact="$GeometryWeapon.bboxdistanceto.{$Control.$target} le $GeometryWeapon.maxfirerange"/>
                    <check_line_of_sight name="$MuzzleLosEx" object="$GeometryWeapon"
                                         objectoffset="$GeometryWeapon.barrelposition"
                                         target="$Control.$target" excludeself="true" useaimtarget="true"/>
                    <!-- $LosEx (no barrel offset, self-excluding) is the diagnostic
                         module's proven clearance signal; the muzzle rays and barrel
                         position are logged as measurements, mirroring observe.xml. -->
                    <check_line_of_sight name="$LosEx" object="$GeometryWeapon"
                                         target="$Control.$target" excludeself="true" useaimtarget="true"/>
                    <check_line_of_sight name="$MuzzleLosSelf" object="$GeometryWeapon"
                                         objectoffset="$GeometryWeapon.barrelposition"
                                         target="$Control.$target" excludeself="false" useaimtarget="true"/>
                    <set_value name="$Barrel" exact="$GeometryWeapon.barrelposition"/>
                    <!-- Gate clear_arc on geometry only. Raycast LOS is unreliable
                         at the spawn instant (ScenarioCommit): a target in empty
                         deep space with excludeself=true reads los_ex/muzzle_los_ex/
                         muzzle_los_self all 0 here, while the settled observe module
                         reports real values. LOS/barrel are logged for the live
                         Mark-moment diagnostic, not used to gate READY. -->
                    <do_if value="($Control.$role == 'clear_arc' and $RootArcPass and $AimArcPass and $InRange)
                                  or ($Control.$role == 'below_arc' and not $RootArcPass and not $AimArcPass
                                      and $RootRelative.rotation.pitch lt -10deg and $AimLocal.pitch lt -10deg and $InRange)">
                      <set_value name="$PassingWeapons" operation="add"/>
                    </do_if>
                    <debug_text text="'[X4GC TEST PREFLIGHT] role=' + $Control.$role + ' target=' + $Control.$target + ' weapon=' + $GeometryWeapon + ' macro=' + $GeometryWeapon.macro + ' root_pitch=' + $RootRelative.rotation.pitch + ' aim_pitch=' + $AimLocal.pitch + ' root_arc_pass=' + $RootArcPass + ' aim_arc_pass=' + $AimArcPass + ' inrange=' + $InRange + ' los_ex=' + $LosEx + ' muzzle_los_ex=' + $MuzzleLosEx + ' muzzle_los_self=' + $MuzzleLosSelf + ' barrel_x=' + $Barrel.x + ' barrel_y=' + $Barrel.y + ' barrel_z=' + $Barrel.z"/>
                  </do_if>
                </do_for_each>
              </do_if>
              <!-- Every operational turret is a beam or plasma; the control must
                   pass for all of them, so expected scales with the turret count. -->
              <do_if value="$PassingWeapons != $GeometryShooter.turrets.operational.count">
                <set_value name="$PreflightFailures" operation="add"/>
                <debug_text text="'[X4GC TEST PREFLIGHT] FAILED role=' + $Control.$role + ' passing_weapons=' + $PassingWeapons + ' expected=' + $GeometryShooter.turrets.operational.count"/>
              </do_if>
            </do_for_each>

            <set_value name="ScenarioRoot.$SpawnedSpecId" exact="ScenarioRoot.$PendingSpecId"/>
            <remove_value name="ScenarioRoot.$Pending"/>
            <remove_value name="ScenarioRoot.$PendingStations"/>
            <debug_text text="'[X4GC TEST SCENARIO] spawned spec id=' + ScenarioRoot.$SpawnedSpecId + ' ships=' + ScenarioRoot.$Spawned.count + ' stations=' + ScenarioRoot.$SpawnedStations.count + ' modules=' + $StationModules + ' turrets=' + $StationTurrets + ' missile_turrets=' + $StationMissileTurrets + ' shields=' + $StationShields + ' engines=' + $StationEngines + ' safe_fixtures=' + $SafeObjectCount + ' safe_weapons=' + $SafeWeapons + ' unsafe_weapons=' + $UnsafeWeapons + ' defence_units=' + $DefenceUnits + ' hostiles=' + $Hostiles + ' repair_fixtures=' + $RepairObjectCount + ' shooters=' + $ShooterCount + ' shooter_weapons=' + $ShooterWeapons + ' shooter_turrets=' + $ShooterTurrets + ' beam=' + $ShooterBeam + ' plasma=' + $ShooterPlasma + ' shooter_missile_turrets=' + $ShooterMissileTurrets + ' guided=' + $ShooterGuided + ' dumbfire=' + $ShooterDumbfire + ' ammo=' + $ShooterAmmo + ' dormant_shooter_weapons=' + $DormantShooterWeapons + ' unsafe_dormant_shooter_weapons=' + $UnsafeDormantShooterWeapons + ' loadout_failures=' + ($StationLoadoutFailures + $LoadoutFailures + $UnsafeDormantShooterWeapons) + ' location_failures=' + $LocationFailures + ' preflight_failures=' + $PreflightFailures + ' geometry_splits=' + $GeometrySplits"/>
            <do_if value="ScenarioRoot.$PendingRequestId != ''">
              <raise_lua_event name="'X4GunneryTestLab.ScenarioReady'"
                               param="'x4gct8:' + ScenarioRoot.$PendingRequestId + ':' + ScenarioRoot.$SpawnedSpecId + ':' + ScenarioRoot.$Spawned.count + ':' + ScenarioRoot.$SpawnedStations.count + ':' + $StationModules + ':' + $StationTurrets + ':' + $StationMissileTurrets + ':' + $StationShields + ':' + $StationEngines + ':' + $SafeObjectCount + ':' + $SafeWeapons + ':' + $UnsafeWeapons + ':' + $DefenceUnits + ':' + $Hostiles + ':' + $RepairObjectCount + ':' + $ShooterCount + ':' + $ShooterMissileTurrets + ':' + $ShooterGuided + ':' + $ShooterDumbfire + ':' + $ShooterAmmo + ':' + ($StationLoadoutFailures + $LoadoutFailures + $UnsafeDormantShooterWeapons) + ':' + $LocationFailures + ':' + $ShooterWeapons + ':' + $ShooterTurrets + ':' + $ShooterBeam + ':' + $ShooterPlasma + ':' + $PreflightFailures + ':' + $GeometrySplits"/>
            </do_if>
            <remove_value name="ScenarioRoot.$PendingRequestId"/>
          </actions>
        </cue>

        <!-- A stale or direct scenario_commit must never replace a fixture
             while the player occupies one of its spawned ships.  The normal
             commit cue has the inverse guard, so exactly one consumes the
             event. -->
        <cue name="ScenarioCommitOccupiedReject" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'scenario_commit'"/>
            <check_value value="ScenarioRoot.$Accepting? and ScenarioRoot.$Accepting"/>
            <check_value value="ScenarioRoot.$Spawned? and ScenarioRoot.$Spawned.indexof.{player.ship}"/>
          </conditions>
          <actions>
            <set_value name="ScenarioRoot.$Accepting" exact="false"/>
            <remove_value name="ScenarioRoot.$Pending"/>
            <remove_value name="ScenarioRoot.$PendingStations"/>
            <remove_value name="ScenarioRoot.$PendingRequestId"/>
            <debug_text text="'[X4GC TEST SCENARIO] replacement rejected; player occupies spawned ship=' + player.ship"/>
          </actions>
        </cue>

        <!-- The first post-action-list census.  A nonzero delay forces this into
             a later MD update while the station remains remote/out of system. -->
        <cue name="StationLoadoutDelayedCensus" instantiate="true">
          <conditions>
            <event_cue_signalled/>
          </conditions>
          <delay exact="1ms"/>
          <actions>
            <do_if value="ScenarioRoot.$GeometryStation? and ScenarioRoot.$GeometryStation != null and ScenarioRoot.$GeometryStation.exists">
              <set_value name="$Station" exact="ScenarioRoot.$GeometryStation"/>
              <set_value name="$DelayedLasers" exact="0"/>
              <do_for_each name="$Weapon" in="$Station.turrets.operational.list">
                <do_if value="$Weapon.macro == macro.turret_xen_m_laser_02_mk1_macro">
                  <set_value name="$DelayedLasers" operation="add"/>
                </do_if>
              </do_for_each>
              <debug_text text="'[X4GC TEST STATION LOADOUT] phase=delayed_1ms request_id=' + ScenarioRoot.$StationLoadoutRequestId + ' station=' + $Station + ' module=' + ScenarioRoot.$StationLoadoutModule + ' modules=' + $Station.modules.operational.count + ' turrets=' + $Station.turrets.operational.count + ' standard_lasers=' + $DelayedLasers + ' missile_turrets=' + $Station.missileturrets.operational.count + ' shields=' + $Station.shields.operational.count + ' engines=' + $Station.engines.operational.count"/>
            </do_if>
            <do_else>
              <debug_text text="'[X4GC TEST STATION LOADOUT] phase=delayed_1ms FAILED reason=station_missing request_id=' + ScenarioRoot.$StationLoadoutRequestId"/>
            </do_else>
          </actions>
        </cue>

        <!-- Repair only after a real player-ship hit. Unlike minimum-hull or
             invulnerability flags, this leaves the fixture attackable and its
             surface elements eligible for the vanilla shoot controller. -->
        <cue name="ScenarioRepairArm" checkinterval="0.1s">
          <conditions>
            <check_value value="ScenarioRoot.$RepairObjects? and ScenarioRoot.$RepairObjects.count"/>
            <check_value value="ScenarioRoot.$PlayerShooters? and ScenarioRoot.$PlayerShooters.count"/>
          </conditions>
          <actions>
            <debug_text text="'[X4GC TEST SCENARIO] repair guard armed shooters=' + ScenarioRoot.$PlayerShooters.count + ' fixtures=' + ScenarioRoot.$RepairObjects.count"/>
          </actions>
          <cues>
            <cue name="ScenarioRepairHit" instantiate="true">
              <conditions>
                <event_object_attacked_object group="ScenarioRoot.$PlayerShooters"/>
                <check_value value="ScenarioRoot.$RepairObjects? and ScenarioRoot.$RepairObjects.indexof.{event.param}"/>
              </conditions>
              <actions>
                <set_object_shield object="event.param" exact="100"/>
                <set_object_hull object="event.param" exact="100"/>
                <do_if value="@event.param3.{1} and event.param3.{1}.exists">
                  <set_object_shield object="event.param3.{1}" exact="100"/>
                  <set_object_hull object="event.param3.{1}" exact="100"/>
                </do_if>
              </actions>
            </cue>
          </cues>
        </cue>

        <!-- Belt-and-braces safety for hostile live-fire fixtures. Station AI
             may revisit weapon modes after initialisation, so Test Lab owns the
             invariant for as long as the spawned station exists. -->
        <cue name="ScenarioSafetyGuard" instantiate="true" checkinterval="1s">
          <conditions>
            <check_value value="ScenarioRoot.$SafeObjects? and ScenarioRoot.$SafeObjects.count"/>
          </conditions>
          <actions>
            <do_for_each name="$Object" in="ScenarioRoot.$SafeObjects">
              <do_if value="$Object.exists">
                <cease_fire object="$Object"/>
                <do_for_each name="$Weapon" in="$Object.turrets.operational.list">
                  <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                </do_for_each>
                <do_for_each name="$Weapon" in="$Object.missileturrets.operational.list">
                  <set_weapon_mode weapon="$Weapon" weaponmode="weaponmode.holdfire"/>
                </do_for_each>
              </do_if>
            </do_for_each>
          </actions>
        </cue>

        <!-- DespawnScenario
             AddUITriggeredEvent("X4GunneryTestLabScenario", "despawn_scenario")
             Manual cleanup.  Also clears the spawned id so the SAME spec can be
             spawned again by a plain Reload UI. -->
        <cue name="DespawnScenario" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'despawn_scenario'"/>
            <check_value value="not (ScenarioRoot.$Spawned? and ScenarioRoot.$Spawned.indexof.{player.ship})"/>
          </conditions>
          <actions>
            <signal_cue_instantly cue="DespawnScenarioImpl"/>
            <do_if value="ScenarioRoot.$SpawnedSpecId?">
              <remove_value name="ScenarioRoot.$SpawnedSpecId"/>
            </do_if>
          </actions>
        </cue>

        <cue name="DespawnScenarioOccupiedReject" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'despawn_scenario'"/>
            <check_value value="ScenarioRoot.$Spawned? and ScenarioRoot.$Spawned.indexof.{player.ship}"/>
          </conditions>
          <actions>
            <debug_text text="'[X4GC TEST SCENARIO] despawn rejected; player occupies spawned ship=' + player.ship"/>
          </actions>
        </cue>

        <!-- DespawnScenarioImpl
             Internal: removes every ship this script created, without explosion.
             Safe when nothing has been spawned.
             Source: cpu_ship_manager.xml:879-894 -->
        <cue name="DespawnScenarioImpl" instantiate="true">
          <conditions>
            <event_cue_signalled/>
          </conditions>
          <actions>
            <do_if value="ScenarioRoot.$Spawned?">
              <do_for_each name="$Ship" in="ScenarioRoot.$Spawned">
                <do_if value="$Ship.exists">
                  <destroy_object object="$Ship" explosion="false"/>
                </do_if>
              </do_for_each>
              <remove_value name="ScenarioRoot.$Spawned"/>
            </do_if>
            <do_if value="ScenarioRoot.$SpawnedStations?">
              <do_for_each name="$Station" in="ScenarioRoot.$SpawnedStations">
                <do_if value="$Station.exists">
                  <destroy_object object="$Station" explosion="false"/>
                </do_if>
              </do_for_each>
              <remove_value name="ScenarioRoot.$SpawnedStations"/>
            </do_if>
            <set_value name="ScenarioRoot.$GeometryStation" exact="null"/>
            <set_value name="ScenarioRoot.$StationLoadoutModule" exact="null"/>
            <set_value name="ScenarioRoot.$StationLoadoutRequestId" exact="''"/>
            <do_if value="ScenarioRoot.$SafeObjects?">
              <remove_value name="ScenarioRoot.$SafeObjects"/>
            </do_if>
            <do_if value="ScenarioRoot.$PlayerShooters?">
              <remove_value name="ScenarioRoot.$PlayerShooters"/>
            </do_if>
            <do_if value="ScenarioRoot.$RepairObjects?">
              <remove_value name="ScenarioRoot.$RepairObjects"/>
            </do_if>
            <do_if value="ScenarioRoot.$GeometryStation?">
              <remove_value name="ScenarioRoot.$GeometryStation"/>
            </do_if>
            <do_if value="ScenarioRoot.$GeometryShooter?">
              <remove_value name="ScenarioRoot.$GeometryShooter"/>
            </do_if>
            <reset_cue cue="ScenarioRepairArm"/>
            <debug_text text="'[X4GC TEST SCENARIO] despawn complete'"/>
          </actions>
        </cue>

        <!-- ArmCapture / CaptureOnEngage
             Automates the ownership-change leg of the Direct-control fallback
             test.  The Test Lab menu refuses to open during an engagement, so
             the owner cannot be flipped by hand at the moment it matters.
             ArmCapture sets a flag instead; CaptureOnEngage then listens for
             the same direct_watch event the main mod's DirectWatch.Arm cue
             listens for.  That event carries the exact ship and target
             components, and the mod only ever emits it when the directed mode
             is attackenemies, so the trigger is self-verifying: if it fires at
             all, the test preconditions held and no one had to check them.
             faction.player is deliberate.  mayattack is false for the player's
             own property absent a fire-authorisation override, which is the
             condition the mod's OwnerChanged cue tests, and it mirrors the real
             case: you board and capture the ship your turrets were shooting. -->
        <cue name="ArmCapture" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'arm_capture'"/>
          </conditions>
          <actions>
            <set_value name="ScenarioRoot.$CaptureArmed" exact="true"/>
            <debug_text text="'[X4GC TEST SCENARIO] capture armed; the next Direct-control engage hands its target to the player 10s in'"/>
          </actions>
        </cue>

        <cue name="CaptureOnEngage" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryControl'" control="'direct_watch'"/>
            <check_value value="ScenarioRoot.$CaptureArmed? and ScenarioRoot.$CaptureArmed"/>
          </conditions>
          <!-- Long enough for the turrets to visibly settle on the target, so
               the roll-off is distinguishable from never having engaged. -->
          <delay exact="10s"/>
          <actions>
            <set_value name="ScenarioRoot.$CaptureArmed" exact="false"/>
            <!-- Same transport contract as the main mod: engine prepends $ to
                 Lua string keys and ids may arrive as raw values. -->
            <set_value name="$Target" exact="@event.param3.$target"/>
            <do_if value="typeof $Target != datatype.component and $Target">
              <set_value name="$Target" exact="component.{$Target}"/>
            </do_if>
            <do_if value="$Target and $Target.exists">
              <debug_text text="'[X4GC TEST SCENARIO] capturing ' + $Target.knownname + ' ' + $Target + ' from owner ' + $Target.owner"/>
              <set_owner object="$Target" faction="faction.player"/>
              <debug_text text="'[X4GC TEST SCENARIO] capture done; owner=' + $Target.owner"/>
            </do_if>
            <do_else>
              <debug_text text="'[X4GC TEST SCENARIO] capture skipped; target no longer exists'"/>
            </do_else>
          </actions>
        </cue>

        <!-- GeometryQualify
             AddUITriggeredEvent("X4GunneryTestLabScenario", "qualify_geometry",
               {requestId = "..."})
             Phase two of the #67 station B discriminator. The owner opens Test
             Lab once after teleporting to the preserved remote Colossus. A
             bounded deterministic search warps the SAME preserved station B in
             the shooter's sector, then GeometryQualifyMeasure waits for a later
             MD update before measuring it. The search never recreates the
             station, changes its exact loadout, or asks the owner to retry a
             coordinate. The timed test still designates the station ROOT.

             r7 live evidence disproved the authored 6000/-800 position but
             found a clear module aim point just outside plasma range. These
             eight closer positions are an experimental search around that
             observation, not a knowledge-base claim. Every trial independently
             records root origin/aim, module origin/aim, range, and external
             muzzle line of fire. Qualification remains fail-closed: the exact
             station census must survive and either a root split or a clear
             module aim-point candidate must exist. -->
        <cue name="GeometryQualify" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabScenario'" control="'qualify_geometry'"/>
          </conditions>
          <actions>
            <set_value name="ScenarioRoot.$GeometryQualifyRequestId" exact="if @event.param3.$requestId then event.param3.$requestId else ''"/>
            <set_value name="ScenarioRoot.$GeometrySearchAttempt" exact="1"/>
            <set_value name="ScenarioRoot.$GeometrySearchDistances" exact="[3500m, 3500m, 3750m, 3750m, 4000m, 4000m, 4250m, 4250m]"/>
            <set_value name="ScenarioRoot.$GeometrySearchYs" exact="[-600m, -650m, -650m, -700m, -700m, -750m, -750m, -800m]"/>
            <set_value name="ScenarioRoot.$GeometrySearchCount" exact="ScenarioRoot.$GeometrySearchDistances.count"/>
            <do_if value="ScenarioRoot.$GeometryStation? and ScenarioRoot.$GeometryStation != null and ScenarioRoot.$GeometryStation.exists
                          and ScenarioRoot.$GeometryShooter? and ScenarioRoot.$GeometryShooter != null and ScenarioRoot.$GeometryShooter.exists">
              <set_value name="$TrialDistance" exact="ScenarioRoot.$GeometrySearchDistances.{ScenarioRoot.$GeometrySearchAttempt}"/>
              <set_value name="$TrialY" exact="ScenarioRoot.$GeometrySearchYs.{ScenarioRoot.$GeometrySearchAttempt}"/>
              <warp object="ScenarioRoot.$GeometryStation" sector="ScenarioRoot.$GeometryShooter.sector">
                <position object="ScenarioRoot.$GeometryShooter" x="0m" y="$TrialY" z="$TrialDistance"/>
              </warp>
              <debug_text text="'[X4GC TEST QUALIFY SEARCH] request_id=' + ScenarioRoot.$GeometryQualifyRequestId + ' attempt=' + ScenarioRoot.$GeometrySearchAttempt + '/' + ScenarioRoot.$GeometrySearchCount + ' action=warp distance=' + $TrialDistance + ' y=' + $TrialY + ' station=' + ScenarioRoot.$GeometryStation"/>
            </do_if>
            <do_else>
              <debug_text text="'[X4GC TEST QUALIFY SEARCH] request_id=' + ScenarioRoot.$GeometryQualifyRequestId + ' attempt=1/' + ScenarioRoot.$GeometrySearchCount + ' action=skip_warp reason=preserved_object_missing station=' + (if ScenarioRoot.$GeometryStation? then ScenarioRoot.$GeometryStation else null) + ' shooter=' + (if ScenarioRoot.$GeometryShooter? then ScenarioRoot.$GeometryShooter else null)"/>
            </do_else>
            <signal_cue cue="GeometryQualifyMeasure"/>
          </actions>
        </cue>

        <!-- A nonzero delay makes every post-warp measurement happen in a later
             MD update. x4ep1_mentor_subscription.xml uses the same 1 ms boundary
             after warping a station. This is an ordering control, not a claim
             that remote equipment needs time to materialise (r6 disproved that). -->
        <cue name="GeometryQualifyMeasure" instantiate="true">
          <conditions>
            <event_cue_signalled/>
          </conditions>
          <delay exact="1ms"/>
          <actions>
            <set_value name="$RequestId" exact="ScenarioRoot.$GeometryQualifyRequestId"/>
            <set_value name="$Attempt" exact="ScenarioRoot.$GeometrySearchAttempt"/>
            <set_value name="$SearchCount" exact="ScenarioRoot.$GeometrySearchCount"/>
            <set_value name="$TrialDistance" exact="ScenarioRoot.$GeometrySearchDistances.{$Attempt}"/>
            <set_value name="$TrialY" exact="ScenarioRoot.$GeometrySearchYs.{$Attempt}"/>
            <set_value name="$RootSplits" exact="0"/>
            <set_value name="$ModuleOriginCandidates" exact="0"/>
            <set_value name="$ModuleAimCandidates" exact="0"/>
            <set_value name="$ModuleClearCandidates" exact="0"/>
            <set_value name="$ModulePairs" exact="0"/>
            <set_value name="$Measured" exact="0"/>
            <set_value name="$Equipped" exact="false"/>
            <set_value name="$StandardLasers" exact="0"/>
            <set_value name="$Modules" exact="0"/>
            <set_value name="$Turrets" exact="0"/>
            <set_value name="$MissileTurrets" exact="0"/>
            <set_value name="$Shields" exact="0"/>
            <set_value name="$Engines" exact="0"/>
            <do_if value="ScenarioRoot.$GeometryStation? and ScenarioRoot.$GeometryStation != null and ScenarioRoot.$GeometryStation.exists
                          and ScenarioRoot.$GeometryShooter? and ScenarioRoot.$GeometryShooter != null and ScenarioRoot.$GeometryShooter.exists">
              <set_value name="$Station" exact="ScenarioRoot.$GeometryStation"/>
              <set_value name="$Shooter" exact="ScenarioRoot.$GeometryShooter"/>
              <set_value name="$Modules" exact="$Station.modules.operational.count"/>
              <set_value name="$Turrets" exact="$Station.turrets.operational.count"/>
              <set_value name="$MissileTurrets" exact="$Station.missileturrets.operational.count"/>
              <set_value name="$Shields" exact="$Station.shields.operational.count"/>
              <set_value name="$Engines" exact="$Station.engines.operational.count"/>
              <do_for_each name="$StationWeapon" in="$Station.turrets.operational.list">
                <do_if value="$StationWeapon.macro == macro.turret_xen_m_laser_02_mk1_macro">
                  <set_value name="$StandardLasers" operation="add"/>
                </do_if>
              </do_for_each>
              <set_value name="$Equipped" exact="$Modules == 5 and $Turrets == 2 and $StandardLasers == 2
                                                 and $MissileTurrets == 0 and $Shields == 4 and $Engines == 0"/>
              <do_for_each name="$GeometryWeapon" in="$Shooter.turrets.operational.list">
                <do_if value="$GeometryWeapon.macro == macro.turret_arg_m_beam_02_mk1_macro
                              or $GeometryWeapon.macro == macro.turret_arg_m_plasma_02_mk1_macro">
                  <set_value name="$Measured" operation="add"/>
                  <set_value name="$RootRelative" exact="$Station.relativeposition.{$GeometryWeapon}"/>
                  <create_orientation name="$AimLocal" orientation="look_at"
                                      refobject="$Station" useaimtarget="true">
                    <position object="$GeometryWeapon" space="$GeometryWeapon"/>
                  </create_orientation>
                  <set_value name="$RootArcPass" exact="$RootRelative.rotation.pitch ge -10deg and $RootRelative.rotation.pitch le 90deg"/>
                  <set_value name="$AimArcPass" exact="$AimLocal.pitch ge -10deg and $AimLocal.pitch le 90deg"/>
                  <set_value name="$InRange" exact="$GeometryWeapon.bboxdistanceto.{$Station} le $GeometryWeapon.maxfirerange"/>
                  <check_line_of_sight name="$RootMuzzleLosEx" object="$GeometryWeapon"
                                       objectoffset="$GeometryWeapon.barrelposition"
                                       target="$Station" excludeself="true" useaimtarget="true"/>
                  <check_line_of_sight name="$RootMuzzleLosSelf" object="$GeometryWeapon"
                                       objectoffset="$GeometryWeapon.barrelposition"
                                       target="$Station" excludeself="false" useaimtarget="true"/>
                  <set_value name="$Barrel" exact="$GeometryWeapon.barrelposition"/>
                  <do_if value="not $RootArcPass and $AimArcPass and $InRange">
                    <set_value name="$RootSplits" operation="add"/>
                  </do_if>
                  <set_value name="$HasModuleOriginCandidate" exact="false"/>
                  <set_value name="$HasModuleAimCandidate" exact="false"/>
                  <set_value name="$HasModuleClearCandidate" exact="false"/>
                  <do_for_each name="$GeometryModule" in="$Station.modules.operational.list">
                    <set_value name="$ModulePairs" operation="add"/>
                    <set_value name="$ModuleRelative" exact="$GeometryModule.relativeposition.{$GeometryWeapon}"/>
                    <create_orientation name="$ModuleAimLocal" orientation="look_at"
                                        refobject="$GeometryModule" useaimtarget="true">
                      <position object="$GeometryWeapon" space="$GeometryWeapon"/>
                    </create_orientation>
                    <set_value name="$ModuleOriginArcPass" exact="$ModuleRelative.rotation.pitch ge -10deg and $ModuleRelative.rotation.pitch le 90deg"/>
                    <set_value name="$ModuleAimArcPass" exact="$ModuleAimLocal.pitch ge -10deg and $ModuleAimLocal.pitch le 90deg"/>
                    <set_value name="$ModuleInRange" exact="$GeometryWeapon.bboxdistanceto.{$GeometryModule} le $GeometryWeapon.maxfirerange"/>
                    <check_line_of_sight name="$ModuleMuzzleLosEx" object="$GeometryWeapon"
                                         objectoffset="$GeometryWeapon.barrelposition"
                                         target="$GeometryModule" excludeself="true" useaimtarget="true"/>
                    <check_line_of_sight name="$ModuleMuzzleLosSelf" object="$GeometryWeapon"
                                         objectoffset="$GeometryWeapon.barrelposition"
                                         target="$GeometryModule" excludeself="false" useaimtarget="true"/>
                    <set_value name="$ModuleOriginCandidate" exact="not $RootArcPass and $ModuleOriginArcPass and $ModuleInRange"/>
                    <set_value name="$ModuleAimCandidate" exact="not $RootArcPass and $ModuleAimArcPass and $ModuleInRange"/>
                    <set_value name="$ModuleClearCandidate" exact="$ModuleAimCandidate and $ModuleMuzzleLosEx"/>
                    <do_if value="$ModuleOriginCandidate">
                      <set_value name="$HasModuleOriginCandidate" exact="true"/>
                    </do_if>
                    <do_if value="$ModuleAimCandidate">
                      <set_value name="$HasModuleAimCandidate" exact="true"/>
                    </do_if>
                    <do_if value="$ModuleClearCandidate">
                      <set_value name="$HasModuleClearCandidate" exact="true"/>
                    </do_if>
                    <debug_text text="'[X4GC TEST QUALIFY MODULE] request_id=' + $RequestId + ' attempt=' + $Attempt + '/' + $SearchCount + ' trial_distance=' + $TrialDistance + ' trial_y=' + $TrialY + ' station=' + $Station + ' station_id=' + $Station.idcode + ' module=' + $GeometryModule + ' module_macro=' + $GeometryModule.macro + ' weapon=' + $GeometryWeapon + ' weapon_macro=' + $GeometryWeapon.macro + ' root_pitch=' + $RootRelative.rotation.pitch + ' module_origin_pitch=' + $ModuleRelative.rotation.pitch + ' module_aim_pitch=' + $ModuleAimLocal.pitch + ' root_arc_pass=' + $RootArcPass + ' module_origin_arc_pass=' + $ModuleOriginArcPass + ' module_aim_arc_pass=' + $ModuleAimArcPass + ' inrange=' + $ModuleInRange + ' range=' + $GeometryWeapon.bboxdistanceto.{$GeometryModule} + ' maxrange=' + $GeometryWeapon.maxfirerange + ' muzzle_los_ex=' + $ModuleMuzzleLosEx + ' muzzle_los_self=' + $ModuleMuzzleLosSelf + ' origin_candidate=' + $ModuleOriginCandidate + ' aim_candidate=' + $ModuleAimCandidate + ' clear_candidate=' + $ModuleClearCandidate + ' barrel_x=' + $Barrel.x + ' barrel_y=' + $Barrel.y + ' barrel_z=' + $Barrel.z"/>
                  </do_for_each>
                  <do_if value="$HasModuleOriginCandidate">
                    <set_value name="$ModuleOriginCandidates" operation="add"/>
                  </do_if>
                  <do_if value="$HasModuleAimCandidate">
                    <set_value name="$ModuleAimCandidates" operation="add"/>
                  </do_if>
                  <do_if value="$HasModuleClearCandidate">
                    <set_value name="$ModuleClearCandidates" operation="add"/>
                  </do_if>
                  <debug_text text="'[X4GC TEST QUALIFY ROOT] request_id=' + $RequestId + ' attempt=' + $Attempt + '/' + $SearchCount + ' trial_distance=' + $TrialDistance + ' trial_y=' + $TrialY + ' station=' + $Station + ' station_id=' + $Station.idcode + ' weapon=' + $GeometryWeapon + ' macro=' + $GeometryWeapon.macro + ' root_pitch=' + $RootRelative.rotation.pitch + ' root_aim_pitch=' + $AimLocal.pitch + ' root_arc_pass=' + $RootArcPass + ' root_aim_arc_pass=' + $AimArcPass + ' inrange=' + $InRange + ' range=' + $GeometryWeapon.bboxdistanceto.{$Station} + ' maxrange=' + $GeometryWeapon.maxfirerange + ' muzzle_los_ex=' + $RootMuzzleLosEx + ' muzzle_los_self=' + $RootMuzzleLosSelf + ' barrel_x=' + $Barrel.x + ' barrel_y=' + $Barrel.y + ' barrel_z=' + $Barrel.z + ' module_origin_candidate=' + $HasModuleOriginCandidate + ' module_aim_candidate=' + $HasModuleAimCandidate + ' module_clear_candidate=' + $HasModuleClearCandidate"/>
                </do_if>
              </do_for_each>
              <debug_text text="'[X4GC TEST STATION LOADOUT] phase=insystem request_id=' + $RequestId + ' attempt=' + $Attempt + '/' + $SearchCount + ' station=' + $Station + ' module=' + ScenarioRoot.$StationLoadoutModule + ' modules=' + $Modules + ' turrets=' + $Turrets + ' standard_lasers=' + $StandardLasers + ' missile_turrets=' + $MissileTurrets + ' shields=' + $Shields + ' engines=' + $Engines + ' equipped=' + $Equipped"/>
              <debug_text text="'[X4GC TEST QUALIFY] station_census request_id=' + $RequestId + ' attempt=' + $Attempt + '/' + $SearchCount + ' trial_distance=' + $TrialDistance + ' trial_y=' + $TrialY + ' station=' + $Station + ' station_id=' + $Station.idcode + ' modules=' + $Modules + ' turrets=' + $Turrets + ' missile_turrets=' + $MissileTurrets + ' shields=' + $Shields + ' engines=' + $Engines + ' equipped=' + $Equipped + ' root_splits=' + $RootSplits + ' module_origin_candidates=' + $ModuleOriginCandidates + ' module_aim_candidates=' + $ModuleAimCandidates + ' module_clear_candidates=' + $ModuleClearCandidates + ' measured=' + $Measured + ' module_pairs=' + $ModulePairs"/>
            </do_if>
            <do_else>
              <debug_text text="'[X4GC TEST QUALIFY] FAILED request_id=' + $RequestId + ' attempt=' + $Attempt + '/' + $SearchCount + ' reason=preserved_object_missing station=' + (if ScenarioRoot.$GeometryStation? then ScenarioRoot.$GeometryStation else null) + ' shooter=' + (if ScenarioRoot.$GeometryShooter? then ScenarioRoot.$GeometryShooter else null)"/>
            </do_else>
            <set_value name="$Qualified" exact="$Equipped and ($RootSplits ge 1 or $ModuleClearCandidates ge 1)"/>
            <do_if value="not $Qualified and $Equipped and $Attempt lt $SearchCount">
              <set_value name="ScenarioRoot.$GeometrySearchAttempt" operation="add"/>
              <set_value name="$NextAttempt" exact="ScenarioRoot.$GeometrySearchAttempt"/>
              <set_value name="$NextDistance" exact="ScenarioRoot.$GeometrySearchDistances.{$NextAttempt}"/>
              <set_value name="$NextY" exact="ScenarioRoot.$GeometrySearchYs.{$NextAttempt}"/>
              <warp object="ScenarioRoot.$GeometryStation" sector="ScenarioRoot.$GeometryShooter.sector">
                <position object="ScenarioRoot.$GeometryShooter" x="0m" y="$NextY" z="$NextDistance"/>
              </warp>
              <debug_text text="'[X4GC TEST QUALIFY SEARCH] request_id=' + $RequestId + ' attempt=' + $NextAttempt + '/' + $SearchCount + ' action=warp distance=' + $NextDistance + ' y=' + $NextY + ' station=' + ScenarioRoot.$GeometryStation"/>
              <signal_cue cue="GeometryQualifyMeasure"/>
            </do_if>
            <do_else>
              <debug_text text="'[X4GC TEST QUALIFY SEARCH] request_id=' + $RequestId + ' attempt=' + $Attempt + '/' + $SearchCount + ' action=' + (if $Qualified then 'qualified' else 'exhausted') + ' equipped=' + $Equipped + ' root_splits=' + $RootSplits + ' module_clear_candidates=' + $ModuleClearCandidates"/>
              <raise_lua_event name="'X4GunneryTestLab.GeometryQualified'"
                               param="'x4gcq5:' + $RequestId + ':' + (if $Qualified then 1 else 0) + ':' + (if $Equipped then 1 else 0) + ':' + $Attempt + ':' + $SearchCount + ':' + $RootSplits + ':' + $ModuleOriginCandidates + ':' + $ModuleAimCandidates + ':' + $ModuleClearCandidates + ':' + $Measured + ':' + $ModulePairs + ':' + $Modules + ':' + $Turrets + ':' + $StandardLasers + ':' + $MissileTurrets + ':' + $Shields + ':' + $Engines"/>
              <remove_value name="ScenarioRoot.$GeometryQualifyRequestId"/>
              <remove_value name="ScenarioRoot.$GeometrySearchAttempt"/>
              <remove_value name="ScenarioRoot.$GeometrySearchDistances"/>
              <remove_value name="ScenarioRoot.$GeometrySearchYs"/>
              <remove_value name="ScenarioRoot.$GeometrySearchCount"/>
            </do_else>
          </actions>
        </cue>

      </cues>
    </cue>
  </cues>
</mdscript>
