<?xml version="1.0" encoding="UTF-8"?>
<!--
  Gunnery Control Test Lab - Fire-Control Observability Logger
  Developer-only.  Belongs in testlab, NOT in the shipped mod.

  PURPOSE
  docs/FIRE_CONTROL_BEHAVIOR.md carries rows marked UNTESTED and UNKNOWN.
  This script answers those during ordinary free play so a capture can be read
  back and a row promoted to LIVE.

  WHY THIS IS EVENT DRIVEN AND NOT A POLLING SWEEP
  The first version of this file polled every turret once a second and logged
  mode, two raycasts, range and rotation.  It ran for ten minutes on a live
  ship, produced 7252 per-turret samples, and could not answer the question at
  all.  Three findings from that capture killed the design, all now recorded in
  the research KB, and all three are why the code below looks nothing like it:

    1. $target.lastattacker resolves to the SHIP, never to a turret.  Across the
       whole capture not one of the 14 turret component ids ever appeared.  So
       "the target got hit" could not be attributed to the turret that hit it,
       which is precisely what every open row needs.
    2. $turret.rotation is the MOUNTED BASE, not the live barrel.  Each turret
       reported exactly one distinct yaw for ten minutes, through hard
       maneuvering.  Aim direction carried zero information.
    3. check_line_of_sight with excludeself="false" reported blocked 7252 times
       out of 7252.  Only excludeself="true" carries signal, and by itself it
       cannot separate a turret that held fire from one that had no shot.

  event_object_attacked_object replaces the polling path for ordinary turret
  hits. common.xsd:13058 documents param3 as [attacked component, weapon], and
  ordinary projectile captures return the turret in event.param3.{2}. Shipped
  proof that param3.{2} has the weapon role:
  md/notifications.xml:1515 reads that exact slot and passes it straight into
  change_relation_on_attack weapon=, whose weapon attribute is common.xsd:21620.
  object= is the ATTACKER, not the victim: common.xsd:13058 says so and
  md/cover.xml:271-273 pairs it with event_object_attacked under the comment
  "if we either attack something or are attacked by something".

  This is kill-method-dependent. In the 2026-08-23 issue #65 capture,
  hitbymissile returned the LAUNCHER SHIP in param3.{2}, not the missile turret.
  FIRED still names the exact turret and missile object. One ordinary HIT line
  can therefore carry what the sweep could not, but a missile HIT line cannot
  be used for per-turret attribution.

  MISSILE FLIGHT CHECK
  event_weapon_fired (common.xsd:16836) names the exact emitter in event.object
  and the fired missile in event.param. For missile turrets a second listener
  records that missile after 500 ms: survival plus increasing distance from the
  firing ship is the own-hull-clearance check. missile.target records the
  intended target; it does not prove impact. The general hit event omits the
  missile object, so launch and arbitrary-target missile impact remain separate
  signals.

  THE CENSUS, AND WHY IT SURVIVED THE REWRITE
  Hits are positive evidence only.  The Hold fire row needs the opposite: proof
  that a turret which WAS in holdfire never appeared as an attacker.  That needs
  a denominator, so a mode census still runs, but stripped to modes alone - no
  raycasts, no rotation, no range, one line per sample rather than one per
  turret, every 10s rather than every 1s.

  It sweeps weapons.operational.list and missileturrets.operational.list.  The
  old sweep used only turrets.operational.list and thereby skipped missile
  turrets entirely, which are a separate property list
  (scriptproperties.xml:542-545) - an unmeasured emitter that could have
  accounted for hits nobody could explain.  turret derives from weapon
  (scriptproperties.xml:1461-1463), so the two explicit lists cover regular
  weapons/turrets, missile turrets and main guns without claiming that one
  property list contains all emitters.

  TRANSPORT
  Same flat-scalar contract as the scenario script: the engine prepends '$' to
  every Lua string key (research KB, "Lua->MD AddUITriggeredEvent transport
  contract", live-tested 2026-08-04).  Component ids arrive as components
  because Lua converts them with ConvertStringToLuaID.

    observe_toggle {enabled = true|false}
    observe_mark   {}
    observe_state  {softtgt =, aimtgt =}

  observe_state is only sent while a Direct-control session exists.  In plain
  free play it never arrives and the log simply carries player.target, which is
  correct rather than degraded.

  THE SNAPSHOT
  The old per-second sweep is not gone, it is demand-driven: "Mark moment" now
  fires one burst of per-weapon geometry lines. Besides the established
  root-origin external LOS check, it captures the target in each mount's local
  frame, the reported muzzle position, and two experimental muzzle-origin
  raycasts. This is the minimum single-restart probe for static mechanical arc
  prediction and own-hull masking; none of the experimental fields is promoted
  to a verdict in the logger.

  VOCABULARY
  The snapshot logs los_ex, muzzle LOS, local geometry, inrange and ready as raw
  values and stops there.
  Mapping them to ENGAGEABLE / OUT OF RANGE / CANNOT BEAR / LINE OF FIRE BLOCKED is the
  analyst's job: the engine exposes no arc test at all, so CANNOT BEAR can only
  ever be read off as "aimable and in range yet never firing", which is an
  interpretation and must not be baked into a log line.

  RELOAD NOTE: any change to THIS file requires a full game restart, because
  testlab/ md is only reloadable together with the rest of the extension.
  See docs/RELOADING.md.
-->
<mdscript name="X4GunneryTestLabObserve" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../../../#modding/xsd/md.xsd">
  <cues>
    <cue name="ObserveRoot">
      <actions>
        <!-- Off by default.  A long play session must not flood debug.log
             because the extension happens to be installed; nothing is written
             until the owner arms it from the Test Lab menu. -->
        <set_value name="$Enabled" exact="false"/>
        <set_value name="$MarkCount" exact="0"/>
        <!-- Seeded so the census time guard has something to compare against on
             its very first check.  Derived from player.age rather than written
             as a bare -1 so it carries the time datatype: player.age is a time
             (suffix s, scriptproperties.xml:2396) and comparing a time against
             a plain number is a type error, not a false. -->
        <set_value name="$LastSample" exact="player.age - 1s"/>
      </actions>
      <cues>

        <!-- ObserveToggle
             AddUITriggeredEvent("X4GunneryTestLabObserve", "observe_toggle",
               {enabled = true|false})
             The master switch.  Resetting ObserveArm is what actually starts
             and stops logging: arming re-evaluates ObserveArm's condition,
             which activates or deactivates both listener cues beneath it. -->
        <cue name="ObserveToggle" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabObserve'" control="'observe_toggle'"/>
          </conditions>
          <actions>
            <set_value name="ObserveRoot.$Enabled" exact="@event.param3.$enabled == true"/>
            <debug_text text="'[X4GC TEST OBSERVE] enabled=' + ObserveRoot.$Enabled + ' t=' + player.age"/>
            <reset_cue cue="ObserveArm"/>
          </actions>
        </cue>

        <!-- ObserveMark
             AddUITriggeredEvent("X4GunneryTestLabObserve", "observe_mark")
             Stamps the interesting instant so the window is greppable
             afterwards.  The label is generated here as an incrementing
             counter rather than typed by the owner: mid-combat the whole point
             is one click, and "which mark was that" is answered by reading the
             surrounding lines.  Marks are accepted even while logging is off,
             so a mark is never silently swallowed. -->
        <cue name="ObserveMark" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabObserve'" control="'observe_mark'"/>
          </conditions>
          <actions>
            <set_value name="ObserveRoot.$MarkCount" exact="ObserveRoot.$MarkCount + 1"/>
            <set_value name="$Label" exact="'mark_' + ObserveRoot.$MarkCount"/>
            <debug_text text="'[X4GC TEST MARK] label=' + $Label + ' enabled=' + ObserveRoot.$Enabled + ' t=' + player.age"/>

            <!-- The one-shot engageability snapshot.  This is the sweep that used to
                 run once a second, kept only as a burst fired by hand at the
                 instant the owner is looking at something wrong.

                 It exists because HIT lines answer "what did the turret shoot"
                 but never "why is this one not shooting".  LINE OF FIRE BLOCKED and
                 CANNOT BEAR are indistinguishable from the cockpit - both look like a
                 turret pointed at the target holding its fire - yet the doc
                 says they behave oppositely, CANNOT BEAR switching away and
                 LINE OF FIRE BLOCKED staying put.  los_ex plus inrange is what separates
                 them.

                 Root-origin excludeself="false" was measured returning blocked
                 7252 times out of 7252 and stays removed. The two new muzzle
                 raycasts deliberately use $Weapon.barrelposition as an origin
                 offset: whether that escapes the parent hull is the experiment.
                 Mount rotation is static, which is exactly what the asset-based
                 trainable-arc calculation needs as its reference frame.

                 The whole cost is fourteen raycasts on a button press, so
                 there is no gate on $Enabled: a snapshot must never be lost
                 because logging happened to be switched off. -->
            <do_if value="player.ship">
              <set_value name="$Target" exact="null"/>
              <!-- A live Gunnery Control session supplies the target it
                   actually ordered. It outranks the engine soft target, which
                   can briefly lag during restore or surface-component changes.
                   Free play has no AimTarget, so retain soft target then the
                   hard player target as its fallback chain. -->
              <do_if value="ObserveRoot.$AimTarget? and ObserveRoot.$AimTarget.exists">
                <set_value name="$Target" exact="ObserveRoot.$AimTarget"/>
              </do_if>
              <do_elseif value="ObserveRoot.$SoftTarget? and ObserveRoot.$SoftTarget.exists">
                <set_value name="$Target" exact="ObserveRoot.$SoftTarget"/>
              </do_elseif>
              <do_elseif value="player.target?">
                <set_value name="$Target" exact="player.target"/>
              </do_elseif>

              <do_if value="$Target">
                <do_for_each name="$Weapon" in="player.ship.weapons.operational.list">
                  <check_line_of_sight name="$LosEx" object="$Weapon" target="$Target" excludeself="true" useaimtarget="true"/>
                  <check_line_of_sight name="$MuzzleLosEx" object="$Weapon" objectoffset="$Weapon.barrelposition" target="$Target" excludeself="true" useaimtarget="true"/>
                  <check_line_of_sight name="$MuzzleLosSelf" object="$Weapon" objectoffset="$Weapon.barrelposition" target="$Target" excludeself="false" useaimtarget="true"/>
                  <!-- The engine's own definition of in-range, verbatim from
                       shipped combat AI: bounding-box distance under max fire
                       range, no size term.  There is no arc term anywhere in
                       vanilla's firing-solution test, which is why CANNOT BEAR
                       has to be read off as "aimable but never firing" rather
                       than measured directly.  $Dist keeps the raw
                       component-distance reading for comparison. -->
                  <set_value name="$Dist" exact="$Weapon.distanceto.{$Target}"/>
                  <set_value name="$Relative" exact="$Target.relativeposition.{$Weapon}"/>
                  <set_value name="$Barrel" exact="$Weapon.barrelposition"/>
                  <!-- Compare the raw component-origin bearing with the XSD's
                       weapon-consistent aim-target bearing. The local-space
                       form is the candidate production pitch predicate; this
                       remains diagnostic until a live capture agrees. -->
                  <create_orientation name="$AimLocal" orientation="look_at" refobject="$Target" useaimtarget="true">
                    <position object="$Weapon" space="$Weapon"/>
                  </create_orientation>
                  <create_orientation name="$BboxLocal" orientation="look_at_bbox" refobject="$Target">
                    <position object="$Weapon" space="$Weapon"/>
                  </create_orientation>
                  <debug_text text="'[X4GC TEST SOLUTION] label=' + $Label
                    + ' t=' + player.age
                    + ' weapon=' + $Weapon
                    + ' macro=' + $Weapon.macro
                    + ' mode=' + $Weapon.mode
                    + ' isturret=' + $Weapon.isclass.turret
                    + ' ready=' + $Weapon.isreadytofire
                    + ' los_ex=' + $LosEx
                    + ' muzzle_los_ex=' + $MuzzleLosEx
                    + ' muzzle_los_self=' + $MuzzleLosSelf
                    + ' rel_x=' + $Relative.x
                    + ' rel_y=' + $Relative.y
                    + ' rel_z=' + $Relative.z
                    + ' rel_yaw=' + $Relative.rotation.yaw
                    + ' rel_pitch=' + $Relative.rotation.pitch
                    + ' aim_yaw=' + $AimLocal.yaw
                    + ' aim_pitch=' + $AimLocal.pitch
                    + ' bbox_pitch=' + $BboxLocal.pitch
                    + ' mount_yaw=' + $Weapon.rotation.yaw
                    + ' mount_pitch=' + $Weapon.rotation.pitch
                    + ' mount_roll=' + $Weapon.rotation.roll
                    + ' barrel_x=' + $Barrel.x
                    + ' barrel_y=' + $Barrel.y
                    + ' barrel_z=' + $Barrel.z
                    + ' inrange=' + ($Weapon.bboxdistanceto.{$Target} le $Weapon.maxfirerange)
                    + ' bboxdist=' + $Weapon.bboxdistanceto.{$Target}
                    + ' dist=' + $Dist
                    + ' maxrange=' + $Weapon.maxfirerange"/>
                </do_for_each>
                <do_for_each name="$Weapon" in="player.ship.missileturrets.operational.list">
                  <check_line_of_sight name="$LosEx" object="$Weapon" target="$Target" excludeself="true" useaimtarget="true"/>
                  <check_line_of_sight name="$MuzzleLosEx" object="$Weapon" objectoffset="$Weapon.barrelposition" target="$Target" excludeself="true" useaimtarget="true"/>
                  <check_line_of_sight name="$MuzzleLosSelf" object="$Weapon" objectoffset="$Weapon.barrelposition" target="$Target" excludeself="false" useaimtarget="true"/>
                  <set_value name="$Dist" exact="$Weapon.distanceto.{$Target}"/>
                  <set_value name="$Relative" exact="$Target.relativeposition.{$Weapon}"/>
                  <set_value name="$Barrel" exact="$Weapon.barrelposition"/>
                  <create_orientation name="$AimLocal" orientation="look_at" refobject="$Target" useaimtarget="true">
                    <position object="$Weapon" space="$Weapon"/>
                  </create_orientation>
                  <create_orientation name="$BboxLocal" orientation="look_at_bbox" refobject="$Target">
                    <position object="$Weapon" space="$Weapon"/>
                  </create_orientation>
                  <debug_text text="'[X4GC TEST SOLUTION] label=' + $Label
                    + ' t=' + player.age
                    + ' weapon=' + $Weapon
                    + ' macro=' + $Weapon.macro
                    + ' mode=' + $Weapon.mode
                    + ' isturret=' + $Weapon.isclass.turret
                    + ' ready=' + $Weapon.isreadytofire
                    + ' los_ex=' + $LosEx
                    + ' muzzle_los_ex=' + $MuzzleLosEx
                    + ' muzzle_los_self=' + $MuzzleLosSelf
                    + ' rel_x=' + $Relative.x
                    + ' rel_y=' + $Relative.y
                    + ' rel_z=' + $Relative.z
                    + ' rel_yaw=' + $Relative.rotation.yaw
                    + ' rel_pitch=' + $Relative.rotation.pitch
                    + ' aim_yaw=' + $AimLocal.yaw
                    + ' aim_pitch=' + $AimLocal.pitch
                    + ' bbox_pitch=' + $BboxLocal.pitch
                    + ' mount_yaw=' + $Weapon.rotation.yaw
                    + ' mount_pitch=' + $Weapon.rotation.pitch
                    + ' mount_roll=' + $Weapon.rotation.roll
                    + ' barrel_x=' + $Barrel.x
                    + ' barrel_y=' + $Barrel.y
                    + ' barrel_z=' + $Barrel.z
                    + ' inrange=' + ($Weapon.bboxdistanceto.{$Target} le $Weapon.maxfirerange)
                    + ' bboxdist=' + $Weapon.bboxdistanceto.{$Target}
                    + ' dist=' + $Dist
                    + ' maxrange=' + $Weapon.maxfirerange"/>
                </do_for_each>
                <debug_text text="'[X4GC TEST SOLUTION] label=' + $Label
                  + ' t=' + player.age
                  + ' end tgt=' + $Target
                  + ' tgtname=' + $Target.knownname
                  + ' tgtsize=' + $Target.size
                  + ' pilot=' + (if player.ship.pilot then player.ship.pilot.command.value else 'none')"/>
              </do_if>
              <do_else>
                <debug_text text="'[X4GC TEST SOLUTION] label=' + $Label + ' t=' + player.age + ' end tgt=none'"/>
              </do_else>
            </do_if>
          </actions>
        </cue>

        <!-- ObserveState
             AddUITriggeredEvent("X4GunneryTestLabObserve", "observe_state",
               {softtgt =, aimtgt =})
             The only two facts MD cannot see for itself: the engine SOFT target
             (MD has player.target and no soft equivalent - scriptproperties.xml
             has no player.softtarget at all) and the mod session's
             aimTargetID.  Pushed from Lua only while a
             Direct-control session exists.  These are what make a HIT line
             answer "did it shoot MY target" rather than merely "it shot". -->
        <cue name="ObserveState" instantiate="true">
          <conditions>
            <event_ui_triggered screen="'X4GunneryTestLabObserve'" control="'observe_state'"/>
          </conditions>
          <actions>
            <set_value name="ObserveRoot.$SoftTarget" exact="@event.param3.$softtgt"/>
            <set_value name="ObserveRoot.$AimTarget" exact="@event.param3.$aimtgt"/>
          </actions>
        </cue>

        <!-- ObserveArm
             The gate.  Its children only exist while it has fired, so switching
             logging off genuinely detaches both listeners rather than leaving
             them running behind an if.

             It also binds $Ship once, here, because event_object_attacked_object
             resolves object= when the listening cue activates.  Re-arming on
             every toggle is therefore what keeps the listener pointed at the
             ship the owner is actually flying after a ship change.  Flip the
             switch off and on again after changing ships.

             checkinterval is mandatory here, not a pacing choice.  MD refuses
             to load a cue whose conditions are all non-event:
               "Found non-event condition 'check_value', event condition
                required!"
             which is exactly how the first run of this file failed - the toggle
             logged enabled=1 and then nothing else ever appeared, because both
             listeners hang off a cue the engine had rejected.  One boolean test
             a second is free, and the listeners underneath are events, so they
             still cost nothing between hits. -->
        <cue name="ObserveArm" checkinterval="1s">
          <conditions>
            <check_value value="ObserveRoot.$Enabled"/>
          </conditions>
          <actions>
            <set_value name="$Ship" exact="player.ship"/>
            <create_group groupname="$FiringWeapons"/>
            <add_to_group groupname="$FiringWeapons" list="$Ship.weapons.operational.list"/>
            <add_to_group groupname="$FiringWeapons" list="$Ship.missileturrets.operational.list"/>
            <debug_text text="'[X4GC TEST OBSERVE] armed ship=' + $Ship + ' name=' + $Ship.knownname + ' t=' + player.age"/>
          </actions>
          <cues>

            <!-- A trigger-pull is the prospective arc predicate's ground
                 truth. event.object names the exact emitter; event.param is
                 the projectile, whose rotation is the only shipped route to a
                 real bore direction at the instant of fire. -->
            <cue name="ObserveFired" instantiate="true">
              <conditions>
                <event_weapon_fired group="$FiringWeapons"/>
              </conditions>
              <actions>
                <set_value name="$Aimed" exact="null"/>
                <do_if value="ObserveRoot.$AimTarget? and ObserveRoot.$AimTarget.exists">
                  <set_value name="$Aimed" exact="ObserveRoot.$AimTarget"/>
                </do_if>
                <do_elseif value="ObserveRoot.$SoftTarget? and ObserveRoot.$SoftTarget.exists">
                  <set_value name="$Aimed" exact="ObserveRoot.$SoftTarget"/>
                </do_elseif>
                <do_elseif value="player.target?">
                  <set_value name="$Aimed" exact="player.target"/>
                </do_elseif>
                <set_value name="$TargetBearing" exact="if $Aimed then $Aimed.relativeposition.{event.param} else null"/>
                <debug_text text="'[X4GC TEST FIRED] t=' + player.age
                  + ' weapon=' + event.object
                  + ' macro=' + event.object.macro
                  + ' mode=' + event.object.mode
                  + ' bullet=' + event.param
                  + ' bullet_yaw=' + @event.param.rotation.yaw
                  + ' bullet_pitch=' + @event.param.rotation.pitch
                  + ' bullet_roll=' + @event.param.rotation.roll
                  + ' aimed=' + (if $Aimed then $Aimed else 'none')
                  + ' aim_error_yaw=' + (if $TargetBearing then $TargetBearing.rotation.yaw else 'none')
                  + ' aim_error_pitch=' + (if $TargetBearing then $TargetBearing.rotation.pitch else 'none')
                  + ' aim_distance=' + (if $TargetBearing then $TargetBearing.length else 'none')"/>
                <!-- Issue #69 fired-ray reconstruction (dependency-free research telemetry).
                     Part A is UNCONDITIONAL and needs only the bullet + firing weapon: the
                     actual bore in the weapon mount-local frame (org_wl/fwd_wl -> ray_wl_pitch)
                     is the primary discriminator vs the authored arc, and the zone-frame org/fwd
                     let the offline check confirm the local +z bore convention against the
                     already-logged event.param.rotation (bullet_yaw/pitch). Local +z is the bore
                     axis (barrel_z dominant in QUALIFY WEAPONPOSE). Every $Aimed field is
                     null-safe (@ prefix / if-then-else) so it degrades to 'none' instead of
                     skipping the probe. -->
                <create_position name="$RayOrgWL"   object="event.param" x="0m" y="0m" z="0m"     space="event.object"/>
                <create_position name="$RayFwdWL"   object="event.param" x="0m" y="0m" z="10000m" space="event.object"/>
                <create_position name="$RayOrgZone" object="event.param" x="0m" y="0m" z="0m"/>
                <create_position name="$RayFwdZone" object="event.param" x="0m" y="0m" z="10000m"/>
                <create_orientation name="$RayBearingWL" orientation="look_at" refposition="$RayFwdWL">
                  <position object="event.object" space="event.object"/>
                </create_orientation>
                <debug_text text="'[X4GC TEST FIREDRAY] t=' + player.age
                  + ' weapon=' + event.object + ' weapon_macro=' + event.object.macro
                  + ' bullet=' + event.param + ' aimed=' + (if $Aimed then $Aimed else 'none')
                  + ' aimed_macro=' + (if $Aimed and @$Aimed.macro then $Aimed.macro else 'none')
                  + ' ray_wl_pitch=' + $RayBearingWL.pitch + ' ray_wl_yaw=' + $RayBearingWL.yaw
                  + ' org_wl=' + $RayOrgWL + ' fwd_wl=' + $RayFwdWL
                  + ' org_zone=' + $RayOrgZone + ' fwd_zone=' + $RayFwdZone"/>
                <!-- Part B: aimed-component-local ray + bbox for offline ray-vs-AABB, plus the
                     useaimtarget / look_at_bbox pitches in the same weapon mount-local frame the
                     predicate uses. Guarded null-safe on a reachable macro bounding box; if the
                     designated target exposes none, Part A above still carries the answer. -->
                <do_if value="$Aimed and $Aimed.exists and @$Aimed.macro.boundingbox.max.x">
                  <create_position name="$RayOrgTL" object="event.param" x="0m" y="0m" z="0m"     space="$Aimed"/>
                  <create_position name="$RayFwdTL" object="event.param" x="0m" y="0m" z="10000m" space="$Aimed"/>
                  <create_orientation name="$RayAimUAT" orientation="look_at" refobject="$Aimed" useaimtarget="true">
                    <position object="event.object" space="event.object"/>
                  </create_orientation>
                  <create_orientation name="$RayAimBbox" orientation="look_at_bbox" refobject="$Aimed">
                    <position object="event.object" space="event.object"/>
                  </create_orientation>
                  <debug_text text="'[X4GC TEST FIREDRAY BBOX] t=' + player.age
                    + ' aimed=' + $Aimed + ' aimed_macro=' + $Aimed.macro
                    + ' aim_uat_pitch=' + $RayAimUAT.pitch + ' aim_bbox_pitch=' + $RayAimBbox.pitch
                    + ' org_tl=' + $RayOrgTL + ' fwd_tl=' + $RayFwdTL
                    + ' bbox_center=' + $Aimed.macro.boundingbox.center + ' bbox_max=' + $Aimed.macro.boundingbox.max"/>
                  <!-- Issue #69 LOS-endpoint discriminator (research only). Question: can
                       check_line_of_sight report the ALREADY-PROVEN fired path clear when handed
                       an explicit reachable endpoint on that ray, instead of the forced
                       useaimtarget (~86 deg) reference that currently yields the false 0/1?
                       Build three engine-macro-local endpoints on the reconstructed fired bore
                       ($RayOrgTL -> $RayFwdTL) bracketing its ray-vs-AABB entry, and LOS-probe
                       each with excludeself both ways, plus the useaimtarget baseline. The bore
                       forward point is +10000 m in bullet-local, so |dir|=10000 m and along-ray
                       parameter t maps to metres as t*10000; epsilon = 0.5 m -> dt = 5e-5.
                       AABB (engine-macro-local): hi = boundingbox.max, lo = 2*center - max.
                       Slab entry t = max over axes of the near-plane t. All three bore-direction
                       components are non-zero for this fixture; an axis-aligned bore would need a
                       divide guard. Endpoints lie on the fired ray. Two discriminators probe each
                       endpoint: (1) the exact fired ray, seeded from the observed projectile
                       origin $RayOrgWL (projectile origin in the weapon-local frame), which is
                       the origin that actually fired; and (2) the predictive pre-fire barrel
                       origin event.object.barrelposition, logged as a separate diagnostic to test
                       whether that earlier origin reconstructs the same path. The barrel origin
                       is NOT guaranteed to equal $RayOrgWL, so a blocked barrel-origin LOS is an
                       origin-mismatch signal, not per se an MD LOS API wall. -->
                  <set_value name="$LosDirX" exact="$RayFwdTL.x - $RayOrgTL.x"/>
                  <set_value name="$LosDirY" exact="$RayFwdTL.y - $RayOrgTL.y"/>
                  <set_value name="$LosDirZ" exact="$RayFwdTL.z - $RayOrgTL.z"/>
                  <set_value name="$LosHiX" exact="$Aimed.macro.boundingbox.max.x"/>
                  <set_value name="$LosHiY" exact="$Aimed.macro.boundingbox.max.y"/>
                  <set_value name="$LosHiZ" exact="$Aimed.macro.boundingbox.max.z"/>
                  <set_value name="$LosLoX" exact="2 * $Aimed.macro.boundingbox.center.x - $LosHiX"/>
                  <set_value name="$LosLoY" exact="2 * $Aimed.macro.boundingbox.center.y - $LosHiY"/>
                  <set_value name="$LosLoZ" exact="2 * $Aimed.macro.boundingbox.center.z - $LosHiZ"/>
                  <set_value name="$LosNearX" exact="[($LosLoX - $RayOrgTL.x) / $LosDirX, ($LosHiX - $RayOrgTL.x) / $LosDirX].min"/>
                  <set_value name="$LosNearY" exact="[($LosLoY - $RayOrgTL.y) / $LosDirY, ($LosHiY - $RayOrgTL.y) / $LosDirY].min"/>
                  <set_value name="$LosNearZ" exact="[($LosLoZ - $RayOrgTL.z) / $LosDirZ, ($LosHiZ - $RayOrgTL.z) / $LosDirZ].min"/>
                  <set_value name="$LosEntryT" exact="[[$LosNearX, $LosNearY].max, $LosNearZ].max"/>
                  <!-- Slab exit: far-plane t is the per-axis max, and the segment
                       leaves the AABB at the nearest far plane, so exit t = min over
                       axes. Preserved to the delayed cue so the whole entry->exit
                       segment can be swept, not just the entry epsilon. -->
                  <set_value name="$LosFarX" exact="[($LosLoX - $RayOrgTL.x) / $LosDirX, ($LosHiX - $RayOrgTL.x) / $LosDirX].max"/>
                  <set_value name="$LosFarY" exact="[($LosLoY - $RayOrgTL.y) / $LosDirY, ($LosHiY - $RayOrgTL.y) / $LosDirY].max"/>
                  <set_value name="$LosFarZ" exact="[($LosLoZ - $RayOrgTL.z) / $LosDirZ, ($LosHiZ - $RayOrgTL.z) / $LosDirZ].max"/>
                  <set_value name="$LosExitT" exact="[[$LosFarX, $LosFarY].min, $LosFarZ].min"/>
                  <set_value name="$LosTs" exact="[$LosEntryT - 0.00005, $LosEntryT, $LosEntryT + 0.00005]"/>
                  <set_value name="$LosLabels" exact="['before', 'entry', 'inside']"/>
                  <do_for_each name="$LosT" in="$LosTs" counter="$LosI">
                    <create_position name="$LosEndpoint" object="$Aimed" space="$Aimed"
                                     x="$RayOrgTL.x + $LosT * $LosDirX"
                                     y="$RayOrgTL.y + $LosT * $LosDirY"
                                     z="$RayOrgTL.z + $LosT * $LosDirZ"/>
                    <check_line_of_sight name="$LosExactSelfIncl" object="event.object" objectoffset="$RayOrgWL" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="false"/>
                    <check_line_of_sight name="$LosExactSelfExcl" object="event.object" objectoffset="$RayOrgWL" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="true"/>
                    <check_line_of_sight name="$LosBarrelSelfIncl" object="event.object" objectoffset="event.object.barrelposition" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="false"/>
                    <check_line_of_sight name="$LosBarrelSelfExcl" object="event.object" objectoffset="event.object.barrelposition" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="true"/>
                    <debug_text text="'[X4GC TEST LOSPROBE] label=' + $LosLabels.{$LosI}
                      + ' weapon=' + event.object + ' engine=' + $Aimed
                      + ' t=' + $LosT + ' dist_from_bullet_m=' + ($LosT * 10000)
                      + ' endpoint=' + $LosEndpoint
                      + ' origin_wl=' + $RayOrgWL + ' origin_barrel=' + event.object.barrelposition
                      + ' exact_selfincl=' + $LosExactSelfIncl + ' exact_selfexcl=' + $LosExactSelfExcl
                      + ' barrel_selfincl=' + $LosBarrelSelfIncl + ' barrel_selfexcl=' + $LosBarrelSelfExcl"/>
                  </do_for_each>
                  <check_line_of_sight name="$LosUatIncl" object="event.object" objectoffset="event.object.barrelposition" target="$Aimed" useaimtarget="true" excludeself="false"/>
                  <check_line_of_sight name="$LosUatExcl" object="event.object" objectoffset="event.object.barrelposition" target="$Aimed" useaimtarget="true" excludeself="true"/>
                  <debug_text text="'[X4GC TEST LOSPROBE] label=useaimtarget'
                    + ' weapon=' + event.object + ' engine=' + $Aimed
                    + ' aim_uat_pitch=' + $RayAimUAT.pitch
                    + ' los_selfincl=' + $LosUatIncl + ' los_selfexcl=' + $LosUatExcl"/>
                  <signal_cue_instantly cue="ObserveFiredLosDelayed" param="table[
                    $projectile = event.param, $projectileid = '' + event.param,
                    $weapon = event.object, $weaponid = '' + event.object,
                    $aimed = $Aimed, $aimedid = '' + $Aimed, $firetime = player.age,
                    $rayorgwlx = $RayOrgWL.x, $rayorgwly = $RayOrgWL.y, $rayorgwlz = $RayOrgWL.z,
                    $barrelx = event.object.barrelposition.x, $barrely = event.object.barrelposition.y, $barrelz = event.object.barrelposition.z,
                    $rayorgtlx = $RayOrgTL.x, $rayorgtly = $RayOrgTL.y, $rayorgtlz = $RayOrgTL.z,
                    $rayfwdtlx = $RayFwdTL.x, $rayfwdtly = $RayFwdTL.y, $rayfwdtlz = $RayFwdTL.z,
                    $losentryt = $LosEntryT, $losexitt = $LosExitT
                  ]"/>
                </do_if>
              </actions>
            </cue>

            <!-- Repeat only the explicit-endpoint discriminator after one
                 second. Geometry is transported as scalars so an expired
                 projectile cannot invalidate reconstruction or the LOS query. -->
            <cue name="ObserveFiredLosDelayed" instantiate="true">
              <conditions>
                <event_cue_signalled/>
              </conditions>
              <delay exact="1s"/>
              <actions>
                <set_value name="$Payload" exact="event.param"/>
                <set_value name="$Projectile" exact="$Payload.$projectile"/>
                <set_value name="$ProjectileID" exact="$Payload.$projectileid"/>
                <set_value name="$Weapon" exact="$Payload.$weapon"/>
                <set_value name="$WeaponID" exact="$Payload.$weaponid"/>
                <set_value name="$Aimed" exact="$Payload.$aimed"/>
                <set_value name="$AimedID" exact="$Payload.$aimedid"/>
                <set_value name="$FireTime" exact="$Payload.$firetime"/>
                <do_if value="$Weapon? and $Weapon.exists and $Aimed? and $Aimed.exists">
                  <create_position name="$RayOrgWL" object="$Weapon" space="$Weapon"
                                   x="$Payload.$rayorgwlx" y="$Payload.$rayorgwly" z="$Payload.$rayorgwlz"/>
                  <create_position name="$BarrelOrgWL" object="$Weapon" space="$Weapon"
                                   x="$Payload.$barrelx" y="$Payload.$barrely" z="$Payload.$barrelz"/>
                  <create_position name="$RayOrgTL" object="$Aimed" space="$Aimed"
                                   x="$Payload.$rayorgtlx" y="$Payload.$rayorgtly" z="$Payload.$rayorgtlz"/>
                  <create_position name="$RayFwdTL" object="$Aimed" space="$Aimed"
                                   x="$Payload.$rayfwdtlx" y="$Payload.$rayfwdtly" z="$Payload.$rayfwdtlz"/>
                  <set_value name="$LosEntryT" exact="$Payload.$losentryt"/>
                  <set_value name="$LosExitT" exact="$Payload.$losexitt"/>
                  <set_value name="$LosDirX" exact="$RayFwdTL.x - $RayOrgTL.x"/>
                  <set_value name="$LosDirY" exact="$RayFwdTL.y - $RayOrgTL.y"/>
                  <set_value name="$LosDirZ" exact="$RayFwdTL.z - $RayOrgTL.z"/>
                  <set_value name="$LosTs" exact="[$LosEntryT - 0.00005, $LosEntryT, $LosEntryT + 0.00005]"/>
                  <set_value name="$LosLabels" exact="['before', 'entry', 'inside']"/>
                  <do_for_each name="$LosT" in="$LosTs" counter="$LosI">
                    <create_position name="$LosEndpoint" object="$Aimed" space="$Aimed"
                                     x="$RayOrgTL.x + $LosT * $LosDirX"
                                     y="$RayOrgTL.y + $LosT * $LosDirY"
                                     z="$RayOrgTL.z + $LosT * $LosDirZ"/>
                    <!-- Child-vs-root discriminator: re-express the same endpoint in
                         root-ship space and run one companion ray to $Aimed.ship.
                         This isolates whether a blocked child LOS clears at the
                         root hull, distinguishing intra-ship obstruction from open
                         space. -->
                    <do_if value="@$Aimed.ship and $Aimed.ship.exists">
                      <create_position name="$LosEndpointRoot" object="$Aimed.ship" space="$Aimed.ship" value="$LosEndpoint"/>
                      <check_line_of_sight name="$LosRootExcl" object="$Weapon" objectoffset="$RayOrgWL" target="$Aimed.ship" targetoffset="$LosEndpointRoot" useaimtarget="false" excludeself="true"/>
                    </do_if>
                    <do_else>
                      <set_value name="$LosRootExcl" exact="'skipped'"/>
                    </do_else>
                    <check_line_of_sight name="$LosExactSelfIncl" object="$Weapon" objectoffset="$RayOrgWL" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="false"/>
                    <check_line_of_sight name="$LosExactSelfExcl" object="$Weapon" objectoffset="$RayOrgWL" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="true"/>
                    <check_line_of_sight name="$LosBarrelSelfIncl" object="$Weapon" objectoffset="$BarrelOrgWL" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="false"/>
                    <check_line_of_sight name="$LosBarrelSelfExcl" object="$Weapon" objectoffset="$BarrelOrgWL" target="$Aimed" targetoffset="$LosEndpoint" useaimtarget="false" excludeself="true"/>
                    <debug_text text="'[X4GC TEST LOSPROBE_DELAYED] fire_t=' + $FireTime + ' delayed_t=' + player.age
                      + ' projectile_id=' + $ProjectileID + ' projectile_exists=' + @$Projectile.exists
                      + ' weapon_id=' + $WeaponID + ' weapon=' + $Weapon
                      + ' aimed_id=' + $AimedID + ' aimed=' + $Aimed
                      + ' label=' + $LosLabels.{$LosI} + ' t=' + $LosT
                      + ' endpoint=' + $LosEndpoint
                      + ' origin_wl=' + $RayOrgWL + ' origin_barrel=' + $BarrelOrgWL
                      + ' exact_selfincl=' + $LosExactSelfIncl + ' exact_selfexcl=' + $LosExactSelfExcl
                      + ' barrel_selfincl=' + $LosBarrelSelfIncl + ' barrel_selfexcl=' + $LosBarrelSelfExcl
                      + ' aimed_ship=' + @$Aimed.ship + ' root_excl=' + $LosRootExcl"/>
                  </do_for_each>
                  <!-- Whole-AABB child sweep. The before/entry/inside probes only
                       cover +/-0.5 m of the slab entry, but the projectile may hit
                       engine collision geometry deeper inside the box. Sweep the
                       full entry->exit segment at 21 fixed fractions and run one
                       child (target=$Aimed) excludeself ray per sample. Any
                       child_excl=1 is positive evidence that child LOS accepts the
                       fired ray somewhere the shallow probes missed. -->
                  <set_value name="$LosSegFracs" exact="[0.00, 0.05, 0.10, 0.15, 0.20, 0.25, 0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60, 0.65, 0.70, 0.75, 0.80, 0.85, 0.90, 0.95, 1.00]"/>
                  <do_for_each name="$LosSegFrac" in="$LosSegFracs" counter="$LosSegI">
                    <set_value name="$LosSegT" exact="$LosEntryT + $LosSegFrac * ($LosExitT - $LosEntryT)"/>
                    <create_position name="$LosSegEndpoint" object="$Aimed" space="$Aimed"
                                     x="$RayOrgTL.x + $LosSegT * $LosDirX"
                                     y="$RayOrgTL.y + $LosSegT * $LosDirY"
                                     z="$RayOrgTL.z + $LosSegT * $LosDirZ"/>
                    <check_line_of_sight name="$LosSegExcl" object="$Weapon" objectoffset="$RayOrgWL" target="$Aimed" targetoffset="$LosSegEndpoint" useaimtarget="false" excludeself="true"/>
                    <debug_text text="'[X4GC TEST LOSPROBE_SEGMENT] fire_t=' + $FireTime + ' delayed_t=' + player.age
                      + ' projectile_id=' + $ProjectileID + ' projectile_exists=' + @$Projectile.exists
                      + ' aimed_id=' + $AimedID
                      + ' sample=' + $LosSegI + ' frac=' + $LosSegFrac + ' t=' + $LosSegT
                      + ' endpoint=' + $LosSegEndpoint
                      + ' child_excl=' + $LosSegExcl"/>
                  </do_for_each>
                </do_if>
                <do_else>
                  <debug_text text="'[X4GC TEST LOSPROBE_DELAYED] status=skipped reason=invalid_query_object'
                    + ' fire_t=' + $FireTime + ' delayed_t=' + player.age
                    + ' projectile_id=' + $ProjectileID + ' projectile_exists=' + @$Projectile.exists
                    + ' weapon_id=' + $WeaponID + ' weapon=' + $Weapon + ' weapon_exists=' + @$Weapon.exists
                    + ' aimed_id=' + $AimedID + ' aimed=' + $Aimed + ' aimed_exists=' + @$Aimed.exists"/>
                </do_else>
              </actions>
            </cue>

            <!-- A dumbfire launch alone does not prove that ignoring the
                 firing ship in a diagnostic ray is safe: the missile could
                 immediately collide with its own hull. Preserve the exact
                 turret/missile pair from event_weapon_fired, wait long enough
                 to clear the mount but far less than the fixture flight time,
                 then report survival and displacement. missile.target is a
                 direct launch-to-intended-target link, not an impact link. -->
            <cue name="ObserveMissileFlight" instantiate="true">
              <conditions>
                <event_weapon_fired group="$FiringWeapons"/>
              </conditions>
              <delay exact="500ms"/>
              <actions>
                <do_if value="event.object.class == class.missileturret">
                  <set_value name="$Weapon" exact="event.object"/>
                  <set_value name="$Missile" exact="event.param"/>
                  <set_value name="$Guided" exact="@$Missile.isguided"/>
                  <set_value name="$Target" exact="@$Missile.target"/>
                  <debug_text text="'[X4GC TEST MISSILE FLIGHT] t=' + player.age
                    + ' weapon=' + $Weapon
                    + ' macro=' + $Weapon.macro
                    + ' missile=' + $Missile
                    + ' guided=' + $Guided
                    + ' target=' + (if $Target then $Target else 'none')
                    + ' exists=' + @$Missile.exists
                    + ' shipdist_500ms=' + (if @$Missile.exists then $Missile.distanceto.{$Ship} else 'none')"/>
                </do_if>
              </actions>
            </cue>

            <!-- ObserveHit
                 One line per hit landed BY the player ship. For ordinary
                 projectiles this names the turret; hitbymissile names the
                 launcher ship and must be read with FIRED/FLIGHT lines.

                 event.param3.{2} is the weapon, event.param3.{1} the component
                 of the victim that was struck, event.param the victim itself,
                 event.param2 the kill method (common.xsd:13058). param3.{2} is
                 read through @ because missing or ship-level attribution is
                 survivable and worth logging, not a dead cue.

                 istgt is the field the open rows turn on: it is true when the
                 thing this weapon hit is the target Direct-control was aiming
                 at.  A line with mode=weaponmode.defend and istgt=true closes
                 the Defend row on its own. -->
            <cue name="ObserveHit" instantiate="true">
              <conditions>
                <event_object_attacked_object object="$Ship"/>
              </conditions>
              <actions>
                <set_value name="$Weapon" exact="@event.param3.{2}"/>
                <!-- Every read off $Weapon and event.param is @-guarded.  A
                     failed property lookup does not merely blank one field: MD
                     abandons the expression and writes the debug_text SOURCE to
                     the log instead, so a single bad read turns the line into
                     unparseable garbage.  Seen live on missile hits, where the
                     emitter is not in weapons.operational.list and $Weapon.mode
                     does not resolve. -->
                <!-- Same target precedence as Direct-control itself: AimTarget
                     is authoritative while a session is active; soft target
                     and player.target are the free-play fallbacks. -->
                <set_value name="$Aimed" exact="null"/>
                <do_if value="ObserveRoot.$AimTarget? and ObserveRoot.$AimTarget.exists">
                  <set_value name="$Aimed" exact="ObserveRoot.$AimTarget"/>
                </do_if>
                <do_elseif value="ObserveRoot.$SoftTarget? and ObserveRoot.$SoftTarget.exists">
                  <set_value name="$Aimed" exact="ObserveRoot.$SoftTarget"/>
                </do_elseif>
                <do_elseif value="player.target?">
                  <set_value name="$Aimed" exact="player.target"/>
                </do_elseif>
                <debug_text text="'[X4GC TEST HIT] t=' + player.age
                  + ' weapon=' + (if $Weapon then $Weapon else 'none')
                  + ' mode=' + (if $Weapon then @$Weapon.mode else 'none')
                  + ' isturret=' + (if $Weapon then $Weapon.isclass.turret else false)
                  + ' hit=' + event.param
                  + ' hitname=' + @event.param.knownname
                  + ' hitcomp=' + (if @event.param3.{1} then event.param3.{1} else 'none')
                  + ' method=' + event.param2
                  + ' aimed=' + (if $Aimed then $Aimed else 'none')
                  + ' istgt=' + ($Aimed? and (event.param == $Aimed or @event.param3.{1} == $Aimed))"/>
              </actions>
            </cue>

            <!-- ObserveCensus
                 The denominator.  Hits alone are positive evidence; proving a
                 holdfire turret never fired needs proof it was in holdfire at
                 the time, which only a periodic read gives.

                 instantiate plus checkinterval, NOT reset_cue cue="this".  An
                 earlier version combined checkinterval with a trailing
                 self-reset, which froze the game hard and wrote a 4.3 GB
                 debug.log in seconds: the reset re-satisfied the condition
                 immediately, so the cue re-fired without any game time passing
                 (every line carried the identical timestamp 247034.45).
                 instantiate lets the engine own the pacing, which is the
                 shipped idiom - see StationPeaceful_Zoom_VisibilityCheck,
                 .x4-research-cache/extracted/scripts-9.00/md/cinematiccamera.xml:2004.

                 $LastSample is belt and braces on top of that: no sample is ever
                 emitted twice for the same value of player.age, so even if a
                 future edit reintroduces a re-entrant path it cannot run away
                 again.  A freeze that needs the process killed is worth two
                 lines of paranoia.

                 10s, not 1s.  Turret modes change when the owner changes them,
                 not continuously, so this only has to be fine-grained enough to
                 bracket a mode change - and at one line per sample the whole
                 census costs less than the old sweep did in a single second. -->
            <cue name="ObserveCensus" instantiate="true" checkinterval="10s">
              <conditions>
                <check_value value="player.age gt ObserveRoot.$LastSample"/>
              </conditions>
              <actions>
                <set_value name="ObserveRoot.$LastSample" exact="player.age"/>
                <!-- The two lists are deliberate. weapons.operational.list
                     covers regular weapons/turrets; missile turrets are a
                     separate property list (scriptproperties.xml:542-545).
                     Keep both in the denominator. -->
                <set_value name="$Census" exact="''"/>
                <do_for_each name="$Weapon" in="$Ship.weapons.operational.list">
                  <set_value name="$Census" exact="$Census + $Weapon + '=' + $Weapon.mode + ','"/>
                </do_for_each>
                <do_for_each name="$Weapon" in="$Ship.missileturrets.operational.list">
                  <set_value name="$Census" exact="$Census + $Weapon + '=' + $Weapon.mode + ','"/>
                </do_for_each>
                <debug_text text="'[X4GC TEST CENSUS] t=' + player.age
                  + ' pilot=' + (if $Ship.pilot then $Ship.pilot.command.value else 'none')
                  + ' hardtgt=' + (if player.target then player.target else 'none')
                  + ' softtgt=' + (if ObserveRoot.$SoftTarget? and ObserveRoot.$SoftTarget then ObserveRoot.$SoftTarget else 'none')
                  + ' modes=' + $Census"/>
              </actions>
            </cue>

          </cues>
        </cue>

      </cues>
    </cue>
  </cues>
</mdscript>
